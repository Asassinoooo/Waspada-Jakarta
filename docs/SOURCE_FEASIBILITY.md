# SPEC-01 — Source feasibility and retention

**Baseline:** 24 September 2026. **Status:** SPEC-01 design accepted by root review; provider reuse approvals remain open. **Scope:** source onboarding and retention policy for the prototype, not implemented collectors.

Public reachability proves only that an endpoint responded. It does not grant permission to archive, transform, or republish its content. Each source stays disabled for automated acquisition until its owner, remit, access conditions, attribution, reuse rights, refresh limits, and deletion obligations are recorded in the source registry. When a right is unknown, use a labelled fixture; a moderator may record a source URL and review outcome, but must not persist source text or excerpts until the applicable terms allow it. No live collector is implemented.

## Source matrix

The **11 September** column below is the dated observation already in `SOURCE_VERIFICATION_PLAN.md`. **24 September live probe** entries are read-only HTTP checks recorded in this task; the HTTP `Date` header is given in UTC. Published documentation and live responses are identified separately. No story body or linked CAP alert document was fetched in these checks.

| Source and intended mode | Published interface, observed fields and time meaning | Polling and health | Activate / block criteria and fixture fallback |
| --- | --- | --- | --- |
| **BMKG nowcast** — live connector candidate; fixture demo. | Published BMKG open-weather page and the RSS/CAP index at [the nowcast index](https://www.bmkg.go.id/alerts/nowcast/id) / `https://www.bmkg.go.id/alerts/nowcast/id/rss.xml`. The 24 Sep index probe returned HTTP 200 XML; the feed probe returned 7 RSS items. The first two were for Papua and Bengkulu, with `pubDate` and alert links; these do not establish a Jakarta warning. RSS provides `title`, `pubDate`, and `link`. CAP alert detail is expected to carry issuer/message status, `sent`, event, area, and `effective`/`onset`/`expires` times; a detail file was not fetched today, so those mappings must be fixture-checked before activation. Feed publication time is not alert validity. | Prior plan proposes **2 minutes**, conditional on a fresh check of BMKG's published request limit; use conditional requests only if supported, and honor throttling. A timeout, empty feed, or stale fetch updates health only; it never means “all clear” or renews an alert. Flag after 3 consecutive failures and back off. | Activate only after source approval, current attribution/rate terms, CAP status/update/cancel handling, time-zone parsing, and valid/expired fixture cases pass review. Require an in-scope Jakarta area and a usable validity interval; expired or superseded warnings are historical. Until rights or CAP parsing are approved, keep the connector off. Fixture: labelled historical CAP XML with one valid warning, one expired warning, and one update/cancellation. |
| **PetaBencana Jakarta reports** — live API candidate; fixture demo. | Published [report-route documentation](https://docs.petabencana.id/routes/laporan-urun-daya); concrete endpoint: `https://api.petabencana.id/reports?admin=ID-JK&geoformat=geojson`. The route documentation describes Jakarta filtering/GeoJSON and includes `created_at`; map geometry and only documented/present report properties should be retained. A report's observation/creation time is distinct from fetch time; no explicit expiry was established. Live API returned HTTP 200 with `statusCode: 200`, a GeoJSON `FeatureCollection`, and **zero features**. | Prior plan proposes **5 minutes**; send the documented User-Agent and honor any rate limit or retry guidance. An empty FeatureCollection is a valid empty result, not evidence that Jakarta has no incidents. After 3 failed polls, flag and back off; do not expire observations merely because retrieval failed. Without source validity, review fast-changing reports after the plan's **60-minute** observation-age limit. | Preserve PetaBencana/crowdsourced attribution and its report status; do not label the platform's status as independent verification. Activate after reuse, display, privacy, and rate-limit terms are recorded and a non-empty schema fixture is reviewed. The route's availability alone does not establish archival or redistribution rights. Fixture: separate empty response and labelled historical GeoJSON report with geometry, `created_at`, status/type and provider attribution. |
| **ANTARA Metro and Metro Kriminalitas** — manual evidence by default; live RSS discovery only after approval; fixture demo. | [RSS directory](https://www.antaranews.com/rss?mobile=true); feeds: `https://www.antaranews.com/rss/metro.xml` and `https://www.antaranews.com/rss/metro-kriminalitas.xml`. Both probes on 24 Sep returned HTTP 200 and RSS/XML; the feed headers identify copyright. On 11 Sep, both had 20 items. Today's bounded probe did not count or inspect individual items. Expected RSS item metadata (title, canonical link/GUID, publication date, description when present) is discovery metadata; `pubDate` is not event time and RSS has no incident-validity field. The original article was not fetched. | Prior proposal is **10 minutes** only if current publisher conditions allow it. Keep cursors and canonical links; treat feed-window loss as a coverage gap. Back off on `429`, `5xx`, parse errors, or timeout; feed freshness cannot renew the story. | Do not archive or display full article text, images, or substantial excerpts based on feed access alone. Enable even metadata polling only after current RSS terms, copyright/attribution, and allowed storage/display are recorded. Until then, moderators may submit the original URL and a permitted, reviewed excerpt; otherwise retain only the approved URL/review metadata or use a fixture. Fixture: synthetic/historical RSS item with publication time, canonical link, and a single-origin attribution; no copied article body. |
| **Korlantas traffic** — manual URL by default; live RSS discovery candidate after approval; fixture demo. | Official [RSS feed](https://korlantas.polri.go.id/feed/). A 24 Sep probe returned HTTP 200 and 10 RSS items. The first item was a Jakarta traffic-action headline, published `2026-09-24 04:25:08 UTC`, linking to the original Korlantas page; the story body was not fetched. RSS title/link/publication time identify a discovery lead, not a verified road closure or validity interval. Feed covers Indonesia, so Jakarta relevance must be established from the original notice. | Prior proposal is **10 minutes**, subject to Korlantas terms and rate limits. Filter after retrieval without discarding the nationwide cursor. Use backoff and 3-failure health flag; outages or irrelevant headlines never mean roads are clear. | Keep automated retrieval off until site/feed terms, attribution, allowed excerpt storage, and source-specific refresh limits are reviewed. Publish no road effect from a headline alone; require an original item with supported road/route, direction, conditions, and effective period. Fixture: labelled synthetic/historical item containing a specific, conditional diversion and separate start/end times. |
| **Official group notices (school/campus/employer)** — manual official URL is the release mode; fixture demo. | [JakEdu](https://edu.jakarta.go.id/) is an institutional starting point. The 24 Sep portal probe returned HTTP 200 and page title “JakEdu”; no notice feed/API or particular group notice was verified. A moderator supplies the issuer's original notice URL. Record issuer, authenticated official domain/account, affected group(s), place if any, publication time, explicit `valid_from`/`valid_to` if stated, conditions, attribution, and review/fetch times. Missing validity is unknown, not indefinite. | No polling by default. A moderator may re-open a source to check for a correction, but that does not extend validity. For an advisory without an end, use the plan's **24-hour review** rule; fast-changing conditions use the relevant observation-age deadline. | Approve each issuing organization/domain and confirm audience scope from an institutional reference. Record excerpt only if the issuer's terms or permission allow it. No private groups or personal/student data. A group notice is not citywide by default. Fixture: clearly labelled synthetic school/campus notice scoped to named groups and dates; never present it as a current instruction. |

### Comparison with 11 September notes

The PetaBencana result is unchanged in kind: both the 11 Sep note and the 24 Sep API probe returned an empty Jakarta `FeatureCollection`. BMKG remains reachable, but today's first two feed entries are for other provinces; unlike the 11 Sep sampled expired CAP detail, no CAP detail was fetched today, so no current validity conclusion can be made. ANTARA Metro and crime RSS were reachable on both dates; 20-item counts are from 11 Sep only, while today's checks establish HTTP 200/RSS format and copyright metadata, not today's item count or story freshness. Korlantas again returned 10 RSS items; today's first item is Jakarta-relevant, whereas the earlier note only recorded the feed and count. JakEdu's page response establishes a reachable institutional landing page, not access to an official group-notice feed. None of these observations changes a source's approval or license state.

## Cross-source failure and freshness rules

Store `fetched_at` separately from source publication, observation/event, and explicit validity timestamps. Preserve provider time zones. Use `Retry-After` when present, bounded exponential backoff with jitter otherwise, content-type/size checks, and allowlisted redirects. Record last success, last attempted fetch, status/error class, and parser version. Three consecutive failed scheduled polls raise a connector-health alert; successful fetches, `304`s, empty feeds, or a newly built feed do not renew evidence freshness. Do not convert coverage gaps to all-clear states. A correction, retraction, or source revocation invalidates dependent excerpts, chunks, vectors, caches, and claims; remove or review any public claim that no longer has eligible support.

## Proposed retention schedule

These are conservative prototype defaults, subordinate to shorter provider terms, privacy obligations, or a deletion request. A source with unknown rights has **zero retained raw content, excerpts, or vectors** until its permission basis is recorded. This schedule is not a legal determination.

| Record | Proposed maximum | Expiry and deletion behavior |
| --- | --- | --- |
| Source registry and acquisition-health metadata | Approval review at least every 12 months; retain until 24 months after disablement, then delete or re-approve. | Keep only source identity/domain, access/permission decision, poll health, and review dates. Do not put feed/article bodies in logs. |
| Evidence provenance, canonical URL/provider ID, timestamps, content hash, attribution, reviewer decision, and source-to-claim links | 24 months after the last related publication/review. | Keep only fields permitted by source terms. If rights expire earlier, remove content-derived fields and retain a minimal non-content audit tombstone if allowed. |
| Permitted supporting text excerpts and extracted text | While the related event version is current, plus 90 days; hard cap 365 days from fetch. | Shorter source terms win. On correction, expiry, or revocation, remove the superseded text unless a documented right allows historical retention. |
| Raw HTML, XML, PDF, image or other fetched file | No durable copy by default. If permission allows temporary disk use: delete after parsing, with a 24-hour maximum; parser-failure quarantine is capped at 7 days. | Keep outside Git and backups where possible. No source images/video or full-article mirror without explicit permission. Record only an allowed hash and fetch result. |
| Chunks, embeddings/vectors, derived summaries and caches | Same expiry as their permitted source text. | Treat vectors as source-derived content. On deletion/retraction, invalidate serving immediately and purge primary/index/cache copies within 24 hours; verify tombstones on restore. |
| Moderator/publication audit trail | 24 months after action. | Retain actor ID, action, record/version IDs, decision reason and timestamps; exclude source bodies, excerpts and unnecessary personal data. If a source-specific right is shorter, keep only the non-content fields allowed. |
| Encrypted external backups | Target: maximum 30-day retention, only after a no-cost export/storage path is designed and tested. | Neon Free alone provides up to six hours or 1 GB of instant-restore history and one manual snapshot, not a 30-day off-provider backup. Until a free export and deletion-replay rehearsal passes, use synthetic/historical demo data only and do not activate live-source persistence. Backup provider, region and deletion guarantees remain open. |

Deletion cascade: source revision/raw object → extracted text and excerpts → evidence chunks and vectors → caches/index snapshots → dependent draft/public claim and summaries → fixture/evaluation copies if they contain real restricted content. Reassess and withdraw or qualify a published claim if support is removed; deletion never marks an incident resolved. Keep only the minimum deletion/audit tombstone allowed by the applicable terms. Synthetic demo fixtures stay namespaced and visibly labelled; real source material in evaluation fixtures follows the same retention and permissions.

## Open source approvals

Before enabling any scheduled connector, record the accountable publisher, permitted endpoints/redirects, allowed fields and retention, attribution text/link, refresh limit, correction/retraction behavior, and contact or policy reference. Specific unresolved items are BMKG's current exact request/attribution conditions; PetaBencana's text/photo reuse and API-rate terms; ANTARA's RSS metadata and article-excerpt/full-text rights; Korlantas' feed/article extraction and archive rights; issuer-specific group-notice and student/privacy terms; and backup-provider region/deletion behavior. Until each applicable item is resolved, the relevant automated path remains off and the representative fixture is the demo path.

Published interface references and bounded probe timestamps are listed in [REFERENCES.md](../REFERENCES.md). Provider probe failures, if any, are recorded as failures; they are not silently replaced with indexed snippets or guessed timestamps.
