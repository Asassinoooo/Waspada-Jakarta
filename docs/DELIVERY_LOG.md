# Delivery log

## 24 September 2026 — development start

- User authorized proceeding with the plan and keeping agent instructions out of Git.
- `AGENTS.md` remains available locally and is ignored/untracked. Existing Git history was not rewritten.
- Root planner assigned SPEC-01 source feasibility, SPEC-02 domain/contracts and the independent portion of SPEC-03 UX/API to GPT-6 Luna subagents with max reasoning. They used disjoint paths in the shared checkout under the initial workflow; each hit the account usage limit before providing a final branch/commit handoff. Their drafts were locally reviewed and completed by root. This process exception is not being represented as compliant agent branch work, and history will not be rewritten.
- SPEC-03 API acceptance depends on the completed SPEC-02 interface; wireframe work can proceed first.
- Source probes are bounded public reads. No paid provider or deployed collector is enabled. No LaTeX work is included.
- Human interviews, source permission decisions and human adjudication of evaluation labels remain explicit outstanding work; agents cannot substitute invented findings.

Review outcomes and actual checks are appended when each work package is accepted. Planned checks are not passing results.

### SPEC-01 accepted as a design baseline

Root reviewed the source matrix, dated observations, reference entries and retention ADR. Endpoint access is distinguished from reuse permission, feed publication from event validity, and empty responses from all-clear statements. The retention policy covers derived vectors and restore-time deletion handling. Corrected the CAP specification attribution to OASIS and separated design acceptance from live-source activation. `git diff --check` passed for the implementation handoff; this documentation package has no runtime tests or implemented collector. Provider-specific permissions, current rate limits and backup arrangements remain activation gates.

### BOOT1 checkpoint — layered plan and contract review

Root summarized the checkpoint and continued without waiting for approval. The primary architecture is now framed as the five-layer system: dedicated L1 ingestion/cleaning/storage, L2 capability-specific models with spatial/semantic grounding, L3 bounded investigation only for unresolved gaps, L4 deterministic publication/application policy, and L5 evaluation/monitoring. Provenance, privacy, prompt-injection isolation, audit and human review remain cross-cutting.

**Files and artifacts changed:**

- Main plan and product alignment: `SOFTWARE_DEVELOPMENT_PLAN.md`, `PROJECT_PLAN.md`, `ARCHITECTURE.md`, `README.md`, `REFERENCES.md`, `SOURCE_VERIFICATION_PLAN.md`, and this log.
- Source/domain contracts: `docs/SOURCE_FEASIBILITY.md`, `docs/DOMAIN_MODEL.md`, `docs/contracts.schema.json`, `docs/contracts.examples.json`, and `docs/decisions/ADR-003-domain-publication-evidence.md`, `ADR-005-source-retention.md`.
- UI/API boundary: `docs/UX_API_SPEC.md`, `docs/api/openapi.yaml`, and `docs/decisions/ADR-006-moderator-auth.md`.
- Deployment/governance: `docs/decisions/ADR-001-single-server-prototype.md` (historical and superseded), `docs/decisions/ADR-010-cloudflare-neon-free.md`, `docs/decisions/README.md`, `docs/IMPLEMENTATION_BACKLOG.md`, and `docs/ARCHITECTURE_ALIGNMENT.md`.
- Local ignored `AGENTS.md` now requires a dedicated branch/worktree and descriptive commit(s) from every future implementation subagent, with Luna Max as default and Astra xhigh only for a substantive difficulty attempted but unresolved by Luna. WSL Ubuntu-26.04 is the required local project test environment. No agent commit was created for the earlier shared-checkout drafts.

**Deployment-plan changes:**

- Superseded the single-VPS/FastAPI sizing proposal; selected React/TypeScript static assets and a TypeScript Worker API, Cloudflare Workflows for bounded background tasks, Neon Free Postgres with PostGIS/pgvector, Hyperdrive, and an optional Workers AI adapter only if a Free-accessible model passes the Indonesian evaluation.
- Recorded current free quotas and fail-closed/degraded behavior. This is a low-volume class demo target, not an always-on or comprehensive Jakarta safety feed.
- Neon Free does not satisfy the previous 30-day external-backup target by itself. Until a free backup/export and deletion-replay path is tested, the plan limits persisted data to synthetic/historical demo records and blocks live-source persistence.
- Added PLATFORM-01 as a WSL-based free-tier compatibility/measurement task. No Cloudflare/Neon account, project, paid model, external service or deployment was created.

**Checks completed on WSL Ubuntu-26.04:**

- Confirmed WSL YAML and JSON Schema tooling are already available; no dependency installation was needed for this review.
- Parsed OpenAPI 3.1: 16 paths, 16 unique operations, 176 local references resolved, path parameters match their templates, and all embedded request/response examples validate against their schemas.
- Checked the JSON Schema Draft 2020-12 schema and validated all 14 synthetic contract records. All passed.
- `git diff --check` passed for tracked changes. New specification documents retain intentional two-space Markdown hard line breaks in metadata; no other trailing-whitespace finding was observed.
- No runtime application or live integration test was run; there is no implementation yet.

**Unresolved decisions:** source reuse/rate permissions; no-cost encrypted backup and deletion replay; model/version and Workers AI free-model access; map tile/gazetteer/geocoder terms; database region/privacy review; actual quota stop thresholds and warm/cold latency; human user interviews and adjudicated case labels. They do not block fixture-backed BOOT-01 implementation, but they block enabling live feeds or making a safety-coverage claim.

SPEC-02 and SPEC-03 are accepted as design baselines, not implemented services. BOOT-01 is the next ready task: implement the WSL-runnable synthetic React/Worker skeleton with mocked sources/models and no cloud credentials. PLATFORM-01 follows that runnable slice. Its implementation must use a fresh Luna Max subagent task branch/worktree and committed handoff when quota availability permits; this checkpoint itself makes no claim that BOOT-01 has started.

### SPEC-03 precision refinement and architecture collateral audit

After the BOOT-01 branch was opened, root tightened the OpenAPI public `TimeScope` projection to discriminate `exact` date-time, `date` date-only, `range`, and `unknown` null values, matching the accepted domain schema. This is committed as `af45d58` (`docs(SPEC-03): enforce time precision in API schema`). WSL revalidation passed again: 16 OpenAPI paths/operations, 176 resolved internal references, path-template parameter checks, and all embedded request/response examples; all 14 synthetic schema examples still validate.

Root text-audited the 12-slide layered kickoff deck and inspected the repository's five-layer SVG. Both keep L1 processing separate, grounding ahead of conditional L3 investigation, the shared L4 publication gate, and L5 oversight. The deck contains no superseded VPS/FastAPI deployment claim, and the SVG remains the logical architecture diagram; neither needed a content change for ADR-010.

### BOOT-01 checkpoint and UI-00 design brief — 24 September 2026

The root prepared [the UI-00 assignment](assignments/UI-00.md) from the requested frontend steer after reviewing the software development plan, backlog, and accepted UX/API specification. The brief defines a Jakarta field-atlas visual direction, six named tokens, accessible contrast pairings, four desktop/mobile/detail/moderator ASCII wireframes, demo/evidence/time rules, read-only moderator scope, and screenshot/accessibility review. UI-00 remains blocked only on the BOOT-01 runnable shell; no UI code or API-contract change was made.

Changed files: `docs/assignments/UI-00.md`, `docs/IMPLEMENTATION_BACKLOG.md`, and `REFERENCES.md`. The references register records the Anthropic frontend-design skill and Vercel web-interface checklist used. Commit `67a8bdb` (`docs(UI-00): define civic interface design brief`) passed `git diff --check`, local Markdown link validation, newline/trailing-whitespace checks in WSL, and a WSL contrast calculation. Root initialized `origin/main` by pushing the reviewed planning checkpoint; the remote had no prior branch heads. No deployment or paid service was used.

BOOT-01 remains active on `work/BOOT-01-runtime-skeleton` in its dedicated worktree. Its scaffold files are being developed, but no agent commit or runtime checks have been handed off yet. WSL can resolve npm but direct outbound HTTPS currently times out; Windows can reach the registry. The project build and verification must remain in WSL, and dependency access is unresolved. The next eligible implementation after BOOT-01 review is UI-00; EVAL-01 still requires human-adjudicated labels and is not fabricated by the team.

### BOOT-01 accepted — 24 September 2026

The root reviewed and integrated BOOT-01 from `work/BOOT-01-runtime-skeleton`. Agent commits: `1421e2497aa08c35f7d694502ab2be494b41678c` (`feat(BOOT-01): scaffold synthetic Worker demo app`) and `21f049e5c348acd7f6f0d04e77c91081fd14b304` (`docs(BOOT-01): record completed implementation handoff`). Root merge: `238128d74b8f97e24fb0392a626bf6e463c05094` (`merge: accept BOOT-01 runtime skeleton`). The change adds a React/TypeScript static demo shell, a modular read-only Worker API with server-selected synthetic data, layer-owned interfaces, fixtures, tests, locked dependencies and a WSL bootstrap guide. It does not add a database, live source, model call, investigation loop, moderator write, cloud resource or API-contract change.

Root checks in WSL Ubuntu-26.04 with Linux Node.js 24.21.0/npm 11.19.0: `npm ci --offline --no-audit --no-fund` passed; `npm run dev` started Vite and local Wrangler without external `Request.cf` access; `npm run smoke` passed against both API routes; `npm test` passed all 6 tests; `npm run typecheck` passed both workspaces; `npm run build` passed Vite production build and Wrangler dry-run. Direct route checks returned HTTP 200 for context/events, stayed demo/synthetic with client dataset parameters, and rejected POST with 405. `git diff --check` passed on the task branch. Direct WSL access to npm was unavailable, so dependency setup used a temporary pass-through CONNECT relay restricted to `registry.npmjs.org:443`; it was stopped after dependency setup and did not persist a WSL/system setting.

BOOT-01 is accepted. UI-00 is the next active implementation on its own `work/UI-00-civic-interface` branch/worktree and uses the approved local synthetic shell. PLATFORM-01 is ready in dependency terms but remains planned while the requested UI slice proceeds. EVAL-01 still needs human-adjudicated labels; live source permissions, data retention/backup, model choice, map/geocoder terms, data region/privacy, quota stop levels and human research remain unresolved.

### UI-00 accepted — 24 September 2026

The root reviewed and merged the UI-00 feature branch into `main`. The implementation commits are `af19a4160858104cd8e44fdf9907b83201d05cd4` (`feat(UI-00): add civic discovery and evidence review`) and `eddb962211b31f378f268f319574ac6c719239c0` (`fix(UI-00): link map to explicit feed selection`). Root merge: `6e1e8a3ad3c0ae3946c4e94e55cbfc2c77f488d4` (`Merge branch 'work/UI-00-civic-interface'`). The detailed branch handoff and findings are in [the UI-00 assignment](assignments/UI-00.md).

Changed paths are limited to `apps/web/**`: the shell, feed/map, event detail, read-only moderator review, local presentation fixture, display helpers, styles, smoke check, and UI tests. The screen slice keeps fixture-only data separate from API records, starts with no map geometry selected, renders only the documented synthetic route after an explicit selection, preserves independent status/time labels, and introduces no moderator writes or API changes.

Root independently reran checks in WSL Ubuntu-26.04 with Node.js 24.21.0/npm 11.19.0: `npm run typecheck` passed; `npm test` passed all 9 tests (5 web, 4 Worker); `npm run smoke` passed against the local Vite/Worker pair; `npm run build` passed the Vite production build and Wrangler dry-run; and `git diff --check` passed. The local UI/API routes returned HTTP 200 during visual review.

Desktop discovery was reviewed at 1440×1000. Discovery/map, detail, and moderator layouts were also reviewed at 390×844; a narrower 720 CSS-pixel desktop viewport showed no horizontal clipping. Screenshots are retained locally under ignored `.codex-build/ui-00-review/`. The critique refined initial map selection, mobile copy/wrapping, URL state, and search scope labels. Remaining limitations are synthetic-only data, no basemap/live source/authentication/publication, and read-only moderator behavior; UI-00 is a front-end fixture slice, not proof of live coverage or safety.

PLATFORM-01 is the next ready task: measure Cloudflare Workers and Neon Free compatibility in WSL and against current official documentation. This work must remain a no-provisioning spike; its results constrain DATA-01 schema decisions. Human evaluation labels and live-source activation gates remain outstanding.

### PLATFORM-01 assigned — 24 September 2026

After pushing the accepted UI-00 checkpoint, root assigned the no-provisioning compatibility spike to a dedicated Luna Max branch/worktree. The scope is recorded in [the PLATFORM-01 assignment](assignments/PLATFORM-01.md). The report must cite current official provider documentation and distinguish hard limits, reproducible local measurements, scenario estimates, and facts that require an authorized provider resource. No Cloudflare or Neon resource, account change, paid service, secret, or live data access is part of the assignment.

### PLATFORM-01 accepted — 24 September 2026

The root reviewed and merged PLATFORM-01 from `work/PLATFORM-01-free-compatibility`. Agent commits: `bc7f414ccaaf9b5cc707022632c439d93fe4080a` (`docs(PLATFORM-01): assess free-tier compatibility`), `839873f9346e20b7b097bc853f8dac7ab73513e3` (`docs(PLATFORM-01): record implementation handoff`), and `4b4908c803f7d93c80d618c6aeb2bee5b23e6e2d` (`docs(PLATFORM-01): align Workflow sensitivity with planned cadences`). Root merge: `80f9636` (`merge: accept PLATFORM-01 compatibility spike`). Changed paths are `docs/PLATFORM_COMPATIBILITY.md`, `docs/decisions/ADR-010-cloudflare-neon-free.md`, `REFERENCES.md`, and the task handoff in `docs/assignments/PLATFORM-01.md`.

The report records current official Cloudflare and Neon documentation accessed 24 September 2026, plus WSL-local fixture measurements for route response sizes/timings and build artifacts. It clearly separates documented limits, local measurements, arithmetic scenarios, and provider facts that remain untested. The project’s proposed source cadences (BMKG 2 minutes, PetaBencana 5 minutes, news/traffic 10 minutes) are preserved as unvalidated proposals. At an illustrative 10 billable Workflow steps/run, their per-schedule estimates are 7,200, 2,880, and 1,440 steps/day; the first exceeds the 3,000-step daily Free allowance. Actual steps/run and shared-versus-separate workflow schedules must be measured before setting source cadence.

The bounded synthetic class-demo target remains plausible from public ceilings, but Cloudflare CPU, Neon query/transfer/latency, Workflows, provider error handling, and Free-project extension activation are not validated. The required 30-day off-provider backup, restore, and deletion-replay path remains absent; persist synthetic/historical demo data only and keep live-source persistence blocked. No external resource, credential, paid usage, account change, live data, or deployment was used.

Agent WSL Ubuntu-26.04 checks passed with Node.js 24.21.0/npm 11.19.0: `npm run typecheck`, `npm test` (9/9), `npm run smoke`, and `npm run build` including Wrangler dry-run. The local Vite proxy briefly logged connection refusals before Wrangler readiness; measured and smoke requests after readiness returned HTTP 200. Root independently ran `git diff --check main..work/PLATFORM-01-free-compatibility` in WSL; it passed. No runtime tests were rerun for the docs-only cadence correction. The detailed measurements and caveats are in [the compatibility report](PLATFORM_COMPATIBILITY.md).

Root acceptance documentation was committed as `53bac0b` (`docs(PLATFORM-01): accept free-tier compatibility spike`) and pushed to `origin/main` on 24 September 2026. DATA-01 is the next ready task; its local-only scope is recorded in [the assignment](assignments/DATA-01.md). It will use the documented ceilings without creating a Neon project or enabling live persistence.

### DATA-01 assigned — 24 September 2026

Root published the [DATA-01 assignment](assignments/DATA-01.md) in `b536eee` (`docs(DATA-01): scope local persistence foundation`) and created `work/DATA-01-postgres-foundation` in its own `.codex-build/worktrees/data-01` worktree. The assignment targets a local PostgreSQL 18-compatible schema, versioned migrations, PostGIS/pgvector support and typed repository ports, with WSL synthetic tests only. It forbids Neon/Hyperdrive provisioning, live-source persistence, secrets, OS-level package installation and API-contract changes. No implementation or database tests are claimed yet; the implementation branch will be reviewed and recorded before integration.

### DATA-01 implementation checkpoint — 24 September 2026

At the user's stop request, DATA-01 was preserved as an unaccepted WIP checkpoint rather than merged. Agent commit `b4bf7e42451f1d1fe13e9758c363e11a13eeec31` (`wip(DATA-01): checkpoint PostgreSQL foundation`) is on `work/DATA-01-postgres-foundation` and was pushed to the matching origin branch. It adds the PostgreSQL migration, migration runner, typed repository ports, local PGlite/PostGIS/pgvector harness and synthetic persistence tests, plus workspace wiring. The detailed status is recorded in [the DATA-01 assignment](assignments/DATA-01.md).

Root ran `npm run typecheck` in WSL Ubuntu-26.04 with Node.js 24.21.0/npm 11.19.0; it passed. Root ran `npm run db:test`; 8/9 tests passed and the vector-distance ordering assertion failed at `apps/db/test/persistence.test.ts:197` (actual `[2,3,2]`, expected `[2,2,3]`). The agent reports `npm ci --offline --no-audit --no-fund` passed. `npm test`, `npm run build`, `npm run smoke`, and `git diff --check` remain unrun. This branch is not merged or accepted; PGlite PostGIS is experimental and does not verify Neon or provider extension compatibility. No OS packages or provider resources were added.

### Checkpoint at user's stop request — 24 September 2026

Implementation work is stopped at the user's request. No subagent is still running. The integrated `main` implementation baseline is `0b46cf7` (`docs(DATA-01): record WIP checkpoint`), already on `origin/main`; this entry records the newer stop-state checkpoint. BOOT-01, UI-00, and PLATFORM-01 remain accepted and integrated; their worktrees are clean. No next implementation task has started.

DATA-01 is preserved as unaccepted work on `work/DATA-01-postgres-foundation`, pushed to `origin` at `b996c72` (`docs(DATA-01): record checkpoint preservation`). It contains the schema/migration runner, repository ports, local PGlite/PostGIS/pgvector test harness and synthetic persistence tests. Follow-up fixes add per-run vector-dimension assertions, gate automatic source acquisition on active/approved policy, separate L1 connector-health privileges from L4 source-policy privileges, reject backdated migrations, and bind embedding input hashes to chunk hashes. Changed paths are the root package manifests plus `apps/db/**` and the DATA-01 assignment handoff. No Worker/API database integration, provider configuration, live source persistence, or API contract change was made.

The DATA-01 handoff reports WSL Ubuntu-26.04 checks with Node.js `v24.21.0` and npm `11.19.0`: `npm run db:test` passed 11/11; `npm test` passed 20/20; `npm run typecheck` and `npm run build` passed; and `git diff --check` passed with explicit WSL Git/worktree paths. `npm run smoke` was not run because API/Worker runtime integration was unchanged. These are agent-reported results; root has not independently rerun the final branch after its last fixes. The branch is clean and pushed but remains unmerged pending root review. The earlier vector-ordering failure in the initial DATA-01 checkpoint was addressed by the branch follow-up; no test result is rewritten or implied for the old revision.

The remaining stop-state constraints are: confirm migration/schema alignment during root review; do not treat PGlite extension behavior as Neon compatibility evidence; keep live-source persistence gated by reuse permission and backup/deletion-replay readiness; and defer JOB-01 until DATA-01 is reviewed. Human-adjudicated evaluation labels and provider/access decisions remain outstanding. The repository is left with clean `main` and a clean, separately checkpointed DATA-01 branch; no merge, deployment, or external resource provisioning occurred.

### DATA-01 accepted — 24 September 2026

After the stop checkpoint was resumed, root reviewed `work/DATA-01-postgres-foundation` through `b996c72` and merged it into `main` as `ed5b5c4` (`merge: accept DATA-01 persistence foundation`). The assignment, backlog, domain model, bootstrap guide, architecture overview and platform compatibility report now distinguish the accepted local persistence foundation from unimplemented Worker/Neon integration. DATA-01 adds the PostgreSQL/PostGIS/pgvector schema, checksum-protected migration runner, typed source/report/trace/audit ports and synthetic PGlite tests. The branch also includes policy/privilege boundary tests, ordered-migration rejection and embedding hash lineage. Changes stayed within the assigned paths.

Root independently ran the final code in WSL Ubuntu-26.04 with native Node.js `v24.21.0` and npm `11.19.0`: `npm run db:test` passed 11/11; `npm test` passed 20/20 (web 5, Worker 4, database 11); `npm run typecheck` passed; `npm run build` passed Vite production build and Wrangler deploy dry-run; `npm run smoke` passed against the local Vite/Worker pair; and `git diff --check main...work/DATA-01-postgres-foundation` passed using explicit WSL Git/worktree paths. The first smoke invocation had no servers running; root then started both documented local services and the rerun passed. No deployment, provider, live-source, or model test was run. The initial WIP vector-ordering failure remains documented as historical; the corrected branch passed.

DATA-01 is accepted only as a local persistence foundation. No Worker DB driver or API wiring exists. Neon/Postgres extension behavior, role-creation privileges on Neon, and provider capacity remain unverified. Report review-state transition history and deletion replay remain LIFE-01 work. The prior stop-state checkpoint remains an accurate historical record as of that point; implementation resumed only after the subsequent user continuation.

### JOB-01 assigned — 24 September 2026

After accepting DATA-01, root recorded [ADR-002](decisions/ADR-002-job-queue-and-scheduler.md) and the [JOB-01 assignment](assignments/JOB-01.md) in `7f76c42` (`docs(JOB-01): define durable queue boundaries`), then created `work/JOB-01-durable-queue` in `.codex-build/worktrees/job-01`. The implementation scope is a local PostgreSQL queue/repository boundary with idempotent scheduled/manual acquisition requests, finite leases/attempts/backoff, crash recovery and connector-health updates. It excludes external scheduler bindings, database drivers, live sources, model calls and public API changes. The implementation agent has not yet returned code or test results.

### User-requested pause checkpoint — 25 September 2026

Implementation was stopped at the user's request and the active JOB-01 subagent was interrupted. The main checkout was clean and synchronized with `origin/main` at `41324d4` before this checkpoint. Root preserved the agent's sole untracked artifact, `apps/db/migrations/002_acquisition_jobs.sql`, with a clear incomplete/unreviewed note in `docs/assignments/JOB-01.md`; both are committed as `0d51db0` (`wip(JOB-01): checkpoint interrupted queue schema`) on `work/JOB-01-durable-queue`. No JOB-01 tests or review were run, and this WIP branch was not pushed or merged.

The full snapshot, integrated components, branches, previously completed checks, outstanding gates, and resume point are recorded in [CURRENT_CHECKPOINT.md](CURRENT_CHECKPOINT.md). Root added only the checkpoint record to `main`; `git diff --check` passed in WSL before commit. No implementation task is continuing.

### Autonomous local-development goal resumed — 25 September 2026

After the pause checkpoint, the user resumed the broader local-development goal. Root verified clean `main` at `da49689`/`origin/main` and clean JOB-01 at `0d51db0`; the JOB-01 Luna Max agent was reactivated to complete the already-scoped queue implementation. No code or checks from its resumed run are accepted yet.

Root split independent parser-contract work from JOB-01 scheduling and EVAL-01's human-label quality gate. The new [ING-PARSE-01 assignment](assignments/ING-PARSE-01.md) covers a bounded, pure PetaBencana-style GeoJSON parser with synthetic fixtures only. The backlog explicitly distinguishes parser contract tests from source-accuracy evaluation and keeps future `ING-01` queue/activation integration dependent on JOB-01, source feasibility, and EVAL-01. Root also separated locally testable L2 contracts into [L2-ADAPTER-01](assignments/L2-ADAPTER-01.md): strict schemas, evidence-bound validators, a clear unconfigured-provider path, and test-only doubles. Real provider selection remains under AI-01 and requires EVAL-01 plus verified provider access. These planning changes alter no public API/domain contract and use no live data, external service, or dependency. `git diff --cached --check` passed for both planning-only changes; no runtime tests were needed. No parser or model adapter code exists yet.

**Checkpoint correction:** Root's current Git audit confirms `4b4908c` is an ancestor of `main`, included through the accepted PLATFORM-01 merge `80f9636`. The earlier checkpoint table incorrectly described that cadence correction as unmerged. The current checkpoint now records the integrated and reviewed state.

### User-requested checkpoint — 25 September 2026

The user asked root to stop active work and record the current state. Root interrupted the JOB-01, ING-PARSE-01, and L2-ADAPTER-01 agents. The latest main checkout was clean at `1ff762e` and synchronized with `origin/main`; no task branch was merged. The agent worktrees were inspected and the unfinished JOB-01 and L2-ADAPTER-01 changes were committed as explicitly unverified root WIP checkpoints so the pause leaves no uncommitted implementation files: `880933d` (`wip(JOB-01): checkpoint moderator insert hardening`) and `62f800e` (`wip(L2-ADAPTER-01): preserve interrupted adapter work`). These commits are local to their task branches and are not accepted implementation commits.

ING-PARSE-01 is clean at `28902fd` and has an agent handoff. The agent reports WSL Worker tests 16/16, workspace tests 32/32, typecheck, build, and diff check passed. The exact parser property names `pkey`, `status`, and `report_type` remain explicitly synthetic and unvalidated against a non-empty provider payload. Root review and independent verification were not performed.

JOB-01 is at `880933d`. The agent reports the earlier implementation revision passed 21/21 DB tests, 30/30 workspace tests, typecheck, build, and diff check. After those reported checks, the agent narrowed L4 queue insert grants to moderator-submission columns and added a role test. That last change was not verified before interruption; the implementation is not accepted. PGlite's single in-memory connection also does not validate true concurrent PostgreSQL sessions.

L2-ADAPTER-01 is at `62f800e`. It contains typed model capability contracts, fail-closed validators, not-configured outcomes, and a test-only double. The agent reports 5/5 focused tests passed and says a generic type error found in the first typecheck was fixed, but the rerun and remaining full checks were interrupted. Root did not run checks or review this branch.

The current branch states, prior accepted components, known limits, and resume order are in [CURRENT_CHECKPOINT.md](CURRENT_CHECKPOINT.md). Runtime checks, external service changes, source reads, model calls, deployment, and cloud provisioning were not performed in this checkpoint.

### ING-PARSE-01 accepted — 25 September 2026

Root reviewed and merged `work/ING-PARSE-01-petabencana-fixtures` through `584f387` as `7d831a5` (`merge: accept ING-PARSE-01 fixture parser`). The parser is a pure Layer 1 transform for already-buffered, bounded GeoJSON and synthetic fixtures. It keeps `created_at` as `sourceCreatedAt` report-record creation metadata, separate from retrieval time, and does not claim that it is when an incident occurred. Empty feeds are explicit empty responses, never an all-clear. The exact `pkey`, `status`, and `report_type` provider mappings remain labelled synthetic assumptions because the observed live feed contained no features and live acquisition remains disabled.

Root independently ran `npm test --workspace=@waspada/worker` (16/16), `npm test` (32/32), `npm run typecheck`, and `npm run build` in WSL Ubuntu-26.04 with Node.js `v24.21.0`/npm `11.19.0`; the build included Vite and Wrangler dry-run. `git diff --check main...HEAD` passed with repository Git from PowerShell. No API route changed, so no smoke run was needed. No live payload, database write, model, source permission, or provider behavior was validated.

### L2-ADAPTER-01 accepted — 25 September 2026

Root reviewed and merged `work/L2-ADAPTER-01-typed-contracts` through `d0be06e` as `b1d1cbe` (`merge: accept L2-ADAPTER-01 typed adapter`). The Worker test script now includes the five focused L2 tests in routine workspace testing. The local adapter separates classification, extraction, embedding, and reasoning; validates untrusted outputs, hashes, vector dimensions and Unicode code-point citations; carries retrieval conflicts/gaps; and returns `not_configured` if no provider is available. Its proposals do not authorize retrieval, tools, source approval, persistence, or publication.

Root independently ran the focused test (5/5), Worker suite (9/9), and full suite (25/25), plus `npm run typecheck` and `npm run build` in WSL Ubuntu-26.04 with Node.js `v24.21.0`/npm `11.19.0`. The build included Vite and Wrangler dry-run. `git diff --check main...HEAD` passed with repository Git from PowerShell. No API route changed. No model/provider was called; these tests establish structural and reference-boundary behavior, not semantic support quality, source accuracy, or provider compatibility.

### User-requested checkpoint — 25 September 2026

The user asked to stop active implementation and record the work completed. Root updated [CURRENT_CHECKPOINT.md](CURRENT_CHECKPOINT.md) and aligned the backlog. `main` is at `b1d1cbe` before this documentation checkpoint, 11 commits ahead of `origin/main` at `9ac2b70`. ING-PARSE-01 and L2-ADAPTER-01 are root-reviewed, independently verified, and merged (`7d831a5`, `b1d1cbe`); their files and root-run checks are detailed in the checkpoint above. The JOB-01 agent completed its branch at `6d92eb0`, including the narrow L4 column grants, minimal enqueue receipt, migration/queue tests, and handoff. The agent reports final WSL results of 21/21 DB tests, 30/30 workspace tests, typecheck, build, and diff check. Root has not independently reviewed or run checks on the final JOB-01 revision, so it remains unaccepted and unmerged. PGlite does not validate concurrent locks across separate PostgreSQL sessions.

At this checkpoint, root documentation changes are limited to `docs/CURRENT_CHECKPOINT.md`, `docs/IMPLEMENTATION_BACKLOG.md`, and `docs/DELIVERY_LOG.md`. No new runtime tests were run for these documentation-only changes; the integration status records the root checks already completed for ING-PARSE-01 and L2-ADAPTER-01. The user explicitly asked not to proceed to another feature task. No live data, model provider, cloud resource, paid service, or deployment was used. The resume point is root review and independent WSL verification of JOB-01.

### JOB-01 accepted and implementation resumed — 25 September 2026

The user resumed the autonomous local-development goal. Root reviewed `work/JOB-01-durable-queue` at `6d92eb0`, including the final migration, typed ports, lease/retry/recovery queries, source-health updates, L4 column privileges, and role-based tests. The branch was merged as `0d49f9e` (`merge: accept JOB-01 durable acquisition queue`). JOB-01 adds a local PostgreSQL queue with dataset-scoped idempotency, source eligibility checks, finite leases/attempts, capped retry, expired-lease recovery, source-health updates, and a restricted L4 moderator-submission receipt. No scheduler binding, source fetching, model, route, or provider configuration was added.

During merged validation, root found the Worker test script and test-file imports obscured which suites ran. Root removed the side-effect import of parser tests from `apps/worker/test/api.test.ts` and listed API, parser, and L2 test files once each in `apps/worker/package.json`, committed as `3a40f60` (`test(worker): run layer suites once from workspace script`). This ensures all accepted Worker suites run once in routine tests.

Root independently ran `npm run db:test` on the final JOB-01 branch (21/21), then `npm test` on merged `main` (47/47: web 5, Worker 21, database 21), `npm run typecheck`, and `npm run build` in WSL Ubuntu-26.04 with Node.js `v24.21.0` and npm `11.19.0`. The build passed Vite and Wrangler deploy dry-run. The root WSL checkout initially lacked installed npm packages; `npm ci --offline --no-audit --no-fund` restored the 88 lockfile dependencies without changing the lockfile, after which the workspace checks passed. Root Git `git diff --check main...work/JOB-01-durable-queue` passed in PowerShell. No smoke test was needed because no runtime route changed.

JOB-01 is accepted as a local implementation. PGlite uses one in-memory database connection, so the tests do not validate concurrent locking across independent PostgreSQL sessions; Neon and hosted Worker behavior remain unverified. No live source, external model, cloud resource, paid service, or deployment was used. Next, root will define a synthetic-only DATA-02 preprocessing/chunk persistence slice that can proceed independently of live acquisition and human evaluation labels.

Root has now split that work into [DATA-02-CORE](assignments/DATA-02-CORE.md) and updated the backlog. This planned local slice covers only deterministic normalization, scoped contact redaction, stable chunking, hashes, and persistence/invalidation using synthetic fixtures and the existing schema. It excludes models, live acquisition, entities/geocoding, vectors, and RAG. The full DATA-02 task remains dependent on later acquisition and evaluation prerequisites.

The assignment was committed in `87452c8` (`docs(DATA-02-CORE): define deterministic text pipeline slice`) and pushed to `origin/main`. GPT-6 Luna Max has started implementation on `work/DATA-02-core-text-pipeline` in `.codex-build/worktrees/data-02-core`; no implementation result is accepted yet.

Root also defined [RAG-CORE](assignments/RAG-CORE.md) as a later local retrieval slice and updated the backlog to separate deterministic evidence candidate search from human-evaluated sufficiency and grounded proposals. RAG-CORE may use only synthetic reports and fixed vectors and must retain contrary evidence, report states, source provenance, and known origin dependencies. It is planned, not assigned; no implementation has started.

### User-requested implementation checkpoint — 25 September 2026

The user asked root to stop the active work and record a checkpoint. Root interrupted the DATA-02-CORE Luna Max agent and preserved its partial, unreviewed work locally on `work/DATA-02-core-text-pipeline` in `.codex-build/worktrees/data-02-core`. The code snapshot is `e5d4fe1` (`wip(DATA-02-CORE): checkpoint interrupted text pipeline`); the assignment handoff is `bccf813` (`docs(DATA-02-CORE): record paused implementation state`). These commits are not pushed or merged. The branch has not been behaviorally reviewed, compiled, or tested. No tests, Worker package-script change, or migration were present at the stop. `git diff --cached --check` passed before the WIP code snapshot commit, and `git diff --check` passed for the handoff.

The WIP adds `apps/worker/src/layers/l1-data-knowledge/text-preparation.ts`, `apps/worker/src/layers/l1-data-knowledge/evidence-chunking.ts`, `apps/db/src/evidence-chunks.ts`, and registration in `apps/db/src/ports.ts`. The root project docs now mark DATA-02-CORE paused, identify the exact branch/commits and unverified status, and retain RAG-CORE as planned but unassigned. Root updated `docs/CURRENT_CHECKPOINT.md`, `docs/IMPLEMENTATION_BACKLOG.md`, `docs/assignments/DATA-02-CORE.md`, and this delivery log; no runtime test was run for these documentation edits.

The main checkout was clean and synchronized with `origin/main` at `4e1531b` before the checkpoint changes. The checkpoint is documentation-only on `main`; its diff check is recorded at commit time. No source/provider access, model call, cloud change, paid service, deployment, or public contract change occurred. The resume point is root review of the preserved DATA-02-CORE WIP, alignment with current `main`, completion of required synthetic tests, and the assignment's WSL checks. PGlite still does not establish locking behavior across independent PostgreSQL sessions.

### DATA-02-CORE resumed; least-privilege scope clarified — 25 September 2026

The user resumed development after the checkpoint. Root merged `main` (`24386a4`) into `work/DATA-02-core-text-pipeline` in `.codex-build/worktrees/data-02-core` as `54b66cb`. The preserved partial text-preparation, chunking, and persistence implementation remains unreviewed while synthetic tests are completed.

The implementation agent reports WSL `npm run db:test` passed 26/26, `npm test` passed 56/56, and `npm run typecheck` passed before the privilege correction. A targeted test under `SET ROLE waspada_l1_pipeline` then failed with SQLSTATE `42501`, `permission denied for table evidence_chunks`, on the atomic persistence statement; no rows were written. This proves that owner-level PGlite tests alone were insufficient: DATA-01 granted the L1 role `INSERT` and status-column `UPDATE`, but no `SELECT` privileges for existing chunk metadata required by collision validation and invalidation.

Root amended [DATA-02-CORE](assignments/DATA-02-CORE.md) to authorize only `apps/db/migrations/003_evidence_chunk_pipeline_reads.sql`, with column-level reads for the exact chunk and embedding metadata used by the repository, plus migration and `SET ROLE` regression tests. The change does not grant table-wide reads, vector or source-text access, or alter migrations 001/002. The assignment, backlog, and checkpoint now reflect this scope. The implementation agent will continue the work and required WSL checks on its branch; no result after this clarification has been accepted or independently verified yet.

### DATA-02-CORE accepted — 25 September 2026

Root reviewed `work/DATA-02-core-text-pipeline` and merged it into `main` as `31bf43e` (`merge: accept DATA-02-CORE text preparation`). The implementation adds deterministic, versioned L1 text preparation and evidence chunking, typed metadata-only persistence with idempotent retries and invalidation, and migration 003's narrowly scoped column-level metadata reads for the L1 role. The change remained within the assignment's allowed paths and scope.

Root independently ran the required checks in WSL Ubuntu-26.04 with Node.js `v24.21.0` and npm `11.19.0`: `npm run db:test` passed 27/27; `npm test` passed 57/57 (web 5, Worker 25, database 27); `npm run typecheck` passed; `npm run build` passed the Vite production build and Wrangler deploy dry-run; and `git diff --check origin/main...HEAD` passed. No runtime route changed, so smoke testing was not required.

The implementation uses only synthetic inputs and does not invoke a source, embedding, or model provider. Contact redaction is pattern-based and incomplete. PGlite does not prove Neon extension compatibility or multi-session locking. No deployment or external service change occurred. RAG-CORE is now the next ready local implementation task.

### RAG-CORE assigned — 25 September 2026

After DATA-02-CORE acceptance, root assigned RAG-CORE on `work/RAG-CORE-hybrid-retrieval` in `.codex-build/worktrees/rag-core`. The task is a bounded, read-only Layer 2 candidate evidence search over existing persisted reports, evidence references, extraction links, source/revision status, supported geometries and origin lineage, with optional semantic distance only for an exact embedding identity. Its local result preserves match facets and timestamps and makes no sufficiency or quality claim. The task does not persist a `GroundingContext`, call an L2 model, start L3, alter public contracts, add a migration/dependency, or provision a service. Implementation is synthetic-only; root will review and independently verify before integration.

### RAG-ACCESS-01 assigned — 25 September 2026

Root review confirmed that DATA-01 defines no suitable read role for the RAG-CORE query: the existing L1 role is a writer with narrow metadata reads, L4 cannot read extraction/chunk/vector rows, and the public role is limited to published projections. The implementation was kept within RAG-CORE's no-migration/no-grant assignment and was tested through the owner-level PGlite executor. To prevent later integration from depending on an owner connection, root accepted [ADR-011](decisions/ADR-011-l2-grounding-reader.md) and assigned [RAG-ACCESS-01](assignments/RAG-ACCESS-01.md): an idempotent `NOLOGIN` group role with only the required column-level reads, tested by running the actual retrieval query under `SET ROLE`. No login, secret, hosted connection, or provider resource is authorized.

### RAG-CORE accepted — 25 September 2026

Root reviewed the task branch's complete diff and merged it into `main` as `5e739cf` (`merge: accept RAG-CORE bounded evidence retrieval`). The module adds bounded typed hybrid retrieval and a thin Worker Layer 2 adapter, with deterministic match facets, source/revision states, contradictory evidence, source/origin lineage, distinct time bases, exact source-linked geometry, optional exact-identity vector distance, and explicit truncation. It makes no sufficiency, quality, or truth claim and does not change schema, roles, dependencies, API, routes, model provider, source behavior, or L3 orchestration.

Root independently verified the merged tree in WSL Ubuntu-26.04 with Node.js `v24.21.0` and npm `11.19.0`: database tests 38/38, all workspace tests 69/69 (web 5, Worker 26, database 38), typecheck, Vite production build, Wrangler deploy dry-run, and WSL `git diff --check` passed. Synthetic tests do not measure retrieval quality/sufficiency or establish Neon behavior. Current DB roles cannot execute the retrieval query; access under a dedicated role is assigned as RAG-ACCESS-01. The application row-evaluation limit does not bound physical DB scanning or establish latency.

### PUB-POLICY-CORE assigned — 25 September 2026

The SDP permits Layer 4 policy foundations to begin against reviewed fixtures before the full M5 integration gate. Root split out [PUB-POLICY-CORE](assignments/PUB-POLICY-CORE.md), a pure deterministic assessment of explicit moderator-reviewed L2 proposals against exact retrieved evidence, source/revision/remit/freshness state, supported geometry, and event-version concurrency. The policy returns per-claim dispositions and reason codes only. Persistence, authentication, automatic publication, learned thresholds, and factual-quality claims stay outside the task; no human labels are fabricated. It was assigned on a separate Worker policy worktree alongside the RAG-ACCESS-01 database privilege work.

### RAG-ACCESS-01 accepted — 25 September 2026

Root reviewed migration 004's exact column-level grants for all 13 RAG-CORE tables and the synthetic role tests, then merged `work/RAG-ACCESS-01-l2-reader` into `main` as `ebedc60`. The implementation proves the actual SQL repository query under `SET ROLE waspada_l2_grounding_reader` for non-semantic, semantic, geometry, and combined paths, with copied-origin dependencies intact. Tests verify exact effective column privileges, no table-level SELECT/write or sequence grants, denied write operations, denied unrelated source/report/audit reads, and migration reapplication.

The task handoff records WSL Ubuntu-26.04 results: database tests 40/40, workspace tests 71/71 (web 5, Worker 26, DB 40), typecheck, Vite/Wrangler build, and diff check passed. Root manually inspected the migration and tests and merged the reviewed branch. Root's independent WSL runtime attempt did not execute: that distro has no Linux Node installation, and its `npm` resolved to Windows Node tooling. The implementation agent's reported WSL checks are therefore the runtime verification evidence for acceptance; the failure before test execution is not counted as a project-test failure or pass. PGlite proves only the local synthetic role boundary. Worker connection membership and hosted Postgres/Neon behavior remain unverified; no login or provider resource was added.

### PUB-POLICY-CORE accepted — 25 September 2026

Root reviewed and merged `work/PUB-POLICY-CORE-manual-gate`. The pure Worker policy returns stable per-claim dispositions and reason codes for an explicit authorized moderator decision; exact support references must match both the validated grounding context and the current RAG result. It checks eligible revisions, approved sources, explicit current in-scope L4 evidence state, source-linked geometry, and event-version concurrency. Historical and synthetic datasets always hold. Contradictions and context evidence remain separately visible and cannot substitute for support. The L2 `updates` relation is retained but fails closed where the current stored relation cannot represent it. No persistence, auth, route, provider, threshold, confidence score or truth claim was added.

The handoff reports WSL Ubuntu-26.04 focused tests 17/17 and full tests 86/86 (web 5, Worker 43, DB 38), plus typecheck, Vite/Wrangler build dry-run, and diff check passing. Root reviewed all changed paths and policy branches; the branch's WSL diff check and test results are recorded in [the assignment](assignments/PUB-POLICY-CORE.md). Root's own WSL instance still lacks Linux Node, so these task-agent results are the runtime test evidence; this environment limitation is also recorded under RAG-ACCESS-01. The tests use authored in-memory fixtures and establish neither factual accuracy, source independence nor retrieval sufficiency. The future persistence service must repeat concurrency checks at transaction time, and MOD-01 must supply real authenticated actor identity.

### EVAL-01-TOOLS assigned — 25 September 2026

Root split the EVAL-01 preparation from human case collection. The assigned tooling task defines a rights-aware casebook contract, keeps independent reviewer inputs separate from adjudication, checks incident-level split leakage, and blocks synthetic or incomplete data from release readiness. It uses authored synthetic validator fixtures only and makes no quality claim. The user identified Perry Tjahya and Jesaya Hamonangan Gaudensius Malau as the independent-reviewer pair. Source-use rights remain pending; no real report acquisition, retention, or labeling is authorized by this input.

### EVAL-01-TOOLS accepted — 25 September 2026

Root reviewed `work/EVAL-01-tools` through `44d325c` and merged it into `main` as `9f80600` (`merge(EVAL-01-TOOLS): accept synthetic casebook tooling`). The implementation adds a closed, versioned metadata-only casebook schema and local validator for authored synthetic or future rights-cleared historical cases. It keeps independent human reviews separate from adjudication, requires report-specific assessments, rejects live datasets and cross-case leakage, and reports independent readiness gates for rights, sample counts, human review, adjudication, held-out freeze, all ten categories, and all four SDP scenarios. Scenario coverage is explicit curator metadata; category coverage counts only labeled categories on reconciled adjudications. No production API, model, source, database, or public contract changed.

Root independently reran the task's checks in WSL Ubuntu-26.04 using Node.js `v24.21.0` and npm `11.19.0`: `npm test` passed 100/100 (web 5, Worker 43, DB 40, casebook 12), `npm run typecheck` passed, and `npm run build` passed Vite and Wrangler deploy dry-run. The task handoff records the linked-worktree `GIT_DIR`/`GIT_WORK_TREE` workaround used for `git diff --check`; root also checked the integrated diff before recording this acceptance.

No real reports, reviewer labels, or source permissions were added. Perry Tjahya and Jesaya Hamonangan Gaudensius Malau are identified as the future reviewer pair, while source/data rights remain pending. The checker cannot authenticate permission records, reviewer identity, label accuracy, representativeness, or the held-out freeze process. EVAL-01 case collection, retention, and labeling therefore remain planned and gated.

### API-PROJECT-CORE assigned — 25 September 2026

After EVAL-01-TOOLS acceptance, root inspected the existing public database views against the public API contract. `public_event_versions` and `public_event_impacts` expose schema 2.0 `record_json` documents containing internal evidence/revision/origin references and decision metadata; the fields required for a public response also need place/service name and source attribution resolution. A role with SELECT on those views must not serialize those JSON objects to clients. Root recorded this boundary in [ADR-012](decisions/ADR-012-public-projection-boundary.md) and assigned [API-PROJECT-CORE](assignments/API-PROJECT-CORE.md), a pure Layer 4 whitelist projector to the unchanged OpenAPI `EventView`.

The task uses only authored in-memory live-shaped values to check public field filtering and fail-closed lookups; no real reports, source rights, or publication decision are implied. It adds no route, database connection, migration, dependency, or contract change. Full API-01 remains gated on publication persistence and later read wiring.

### API-PROJECT-CORE accepted — 25 September 2026

Root reviewed `work/API-PROJECT-CORE` at `64b12fa`, including its mixed-precision time-range correction, and merged the initial implementation and final follow-up into `main` at `c383faa` and `249c5e2`. The pure Layer 4 module validates consumed schema 2.0 Event/Impact fields, resolves scope display names and exact support attributions that explicitly allow public use, verifies exact versioned impacts, and builds only the existing OpenAPI `EventView` keys. It never serializes or spreads storage JSON. The follow-up preserves schema-valid mixed date/date-time range endpoints without inferring order across different precision levels.

Root independently ran WSL Ubuntu-26.04 with Node.js `v24.21.0` and npm `11.19.0`: `npm test` passed 109/109 (web 5, Worker 52, database 40, casebook 12), `npm run typecheck` passed, `npm run build` passed Vite and Wrangler dry-run, and the final branch diff check passed. No endpoint, DB read, contract, dependency, migration, provider, or real source/permission data was added. Source rights remain pending; current tests prove projection behavior only.

Root defined ADR-013 and assigned `PUB-WRITE-CORE` as the next source-independent task: atomically persist explicit manual decisions and versioned event/impact data with idempotency, concurrency checks and a minimal outbox. Human-adjudicated quality evaluation, source rights and HTTP authentication remain future gates.

### PUB-WRITE-CORE accepted — 25 September 2026

The implementation agent completed `work/PUB-WRITE-CORE` in `.codex-build/worktrees/pub-write-core` and committed `3889e756194cd9c4451a1009334bb9c8c4e475e7` (`feat(PUB-WRITE-CORE): persist approved publications atomically`) and `cc7a51b795bae008769d2858cf3aaa4659e9b98e` (`docs(PUB-WRITE-CORE): record implementation handoff`). Root reviewed both commits and merged the branch into `main` as `b2fb994` (`merge(PUB-WRITE-CORE): accept publication writer`). Root approved one narrow scope extension to `apps/db/test/migrations.test.ts`: register migration 005 and update the applied/skipped/version counts.

Migration 005 adds dataset-scoped append-only idempotency receipts and a payload-minimal publication outbox. The typed transactional writer accepts only an explicit trusted-caller moderator approval in the configured `live` namespace, validates exact versions and proposal/evidence/origin/geometry relationships, serializes idempotency/event/impact keys, and atomically writes decisions, event/impact versions and relations, audit, receipt, and outbox. It returns stable replay and version-conflict results. The writer is not exposed over HTTP and does not authenticate the reviewer. Only authored synthetic/live-shaped fixtures are used; no real source rights, reviewer rights or incident quality are asserted.

Root independently ran WSL Ubuntu-26.04 with Node.js `v24.21.0` and npm `11.19.0`: `npm run db:test` passed 50/50; `npm test` passed 119/119 (web 5, Worker 52, database 50, evaluation 12); `npm run typecheck` passed; `npm run build` passed Vite and Wrangler deploy dry-run; and WSL `git diff main...HEAD --check` passed with explicit `GIT_DIR`/`GIT_WORK_TREE` for the linked worktree. The new writer narrows its publication input table reads and adds no UPDATE/DELETE grants, while preserving existing source-policy, trace/audit and acquisition-job grants on the shared L4 role. That shared role is not appropriate for an HTTP runtime without further separation; a dedicated narrower role and MOD-01 authorization are required before route wiring. PGlite does not prove hosted PostgreSQL/Neon transactions or concurrency. Source/data rights remain pending.

### GEO-STORE-CORE assigned — 25 September 2026

Root selected source-backed geometry persistence as the next local dependency after reviewing the accepted data, parser, text/chunk, retrieval, publication, and projection implementations. The schema already has PostGIS geometry and evidence-link tables, but L1 has no typed repository for them. The bounded assignment permits a single forward-only least-privilege migration and synthetic PGlite tests; it forbids source access, geocoding, spatial expansion, publication, and API changes. A support relation is not a claim that geometry semantics have been human-verified. See `docs/assignments/GEO-STORE-CORE.md`.

### GEO-STORE-CORE accepted — 25 September 2026

Root reviewed `work/GEO-STORE-CORE-source-backed-geometry`, including implementation commit `40f1572927dc0c9c48081ec8087b57439e8e7ecf` (`feat(GEO-STORE-CORE): persist source-backed geometry`) and handoff commit `dc733840591c764c193b2a62418e7d1e3674e881` (`docs(GEO-STORE-CORE): record implementation handoff`), then merged it as `3207800` (`merge(GEO-STORE-CORE): accept source-backed geometry`). A separate read-only review found no concrete gap against the assignment. The local L1 writer runtime-validates schema 2.0 Geometry inputs, CRS84 coordinate bounds, role/type compatibility and PostGIS topology, then resolves exact same-dataset `supports` references against immutable report text/hash/code-point spans. It atomically stores the supplied PostGIS shape and geometry-evidence relations; stable retries compare typed columns, JSONB record, EWKB shape, and the entire persisted link set. Migration 006 grants only the specific L1 columns needed for those reads, with no table-wide reads or geometry update/delete privileges.

Root independently reran the implementation in WSL Ubuntu-26.04 using native Linux Node.js `v24.21.0` and npm `11.19.0`. `npm run db:test` passed 58/58; `npm test` passed 127/127 (web 5, Worker 52, DB 58, casebook 12); `npm run typecheck`, `npm run build` (Vite production build and Wrangler dry-run), and `git diff main...HEAD --check` passed. The linked-worktree diff check used explicit `GIT_DIR` and `GIT_WORK_TREE`. An initial `npm ci --offline` attempt encountered a transient NTFS `ENOTEMPTY` cleanup error; a retry installed the locked 88 packages successfully without modifying the lockfile. A concurrent build attempt hit transient memory pressure; the isolated build passed.

All records are authored synthetic fixtures. The implementation verifies storage shape and reference lineage only; it does not establish semantic support, source rights, factual accuracy, or hosted Neon behavior. No contract, API, provider configuration, dependency, or live-source permission changed. The next local prerequisite for fixture-to-L1 integration is to make report revision and evidence-reference persistence retries idempotent; current queue idempotency covers enqueue only, while report writes are plain inserts and evidence-reference inserts receive a new identity on each retry.

### L1-WRITE-IDEMPOTENCY-CORE selected — 25 September 2026

The read-only next-slice audit confirmed that JOB-01 deduplicates enqueues but does not deduplicate L1 records: `ReportRevisionRepository.create` is a plain insert and `createEvidenceReference` allocates a new identity for every retry. Root assigned [L1-WRITE-IDEMPOTENCY-CORE](assignments/L1-WRITE-IDEMPOTENCY-CORE.md) to add race-safe create-or-verify semantics for caller-stable report revision IDs and a database-enforced natural identity for exact evidence references. The task was implemented on `work/L1-WRITE-IDEMPOTENCY-CORE` in `.codex-build/worktrees/l1-write-idempotency-core`, based on main commit `45e283c`, and accepted at merge `59e59e8`. It leaves schema 2.0 and public APIs unchanged, refuses to clean up pre-existing duplicate evidence rows, and proves the existing L1 column grants under synthetic PGlite. It does not yet run the fixture pipeline or claim hosted PostgreSQL concurrency.

### L1-WRITE-IDEMPOTENCY-CORE accepted — 25 September 2026

Root reviewed and integrated `work/L1-WRITE-IDEMPOTENCY-CORE` from implementation commit `c333ecd1bbb2190a2d5d181ce20c9f951f007315` and handoff commit `aa31f9acae66a8420fbb421d030f810d06f111ed` as merge `59e59e8`. `ReportRevisionRepository.create` now inserts against the caller's dataset/revision key and compares every immutable typed field and JSONB `record_json`; same-key payload drift returns stable `ReportRevisionConflictError` (`report_revision_conflict`). Evidence-reference writes verify the stored report hash and Unicode code-point bounds, then use migration 007's natural identity `(dataset_kind, report_revision_id, permitted_text_hash, span_start, span_end, offset_unit, relation)` to return the original ID without replacing its first trace. Migration 007 refuses legacy duplicates before creating its unique index, in the migration runner's transaction, and adds no privileges.

Root authorized one narrow test-only scope extension in `apps/db/test/geometry-writer.test.ts`: the previous ambiguity scenario manually inserted duplicate evidence rows, which became impossible once the natural identity was enforced. The obsolete fixture/assertion was removed; geometry implementation and other support checks did not change. Root independently ran WSL Ubuntu-26.04 with Node.js `v24.21.0` and npm `11.19.0`: `npm run db:test` passed 60/60, `npm test` passed 129/129 (web 5, Worker 52, database 60, evaluation 12), `npm run typecheck` passed, `npm run build` passed Vite and Wrangler dry-run, and `git diff --check` passed. The PGlite suite uses a single local session, so it does not establish multi-session PostgreSQL/Neon locking or concurrency. Migration 007 must stop for operator/root disposition if legacy duplicate identities exist. Synthetic tests establish retry and storage invariants only, not semantic support, source rights or factual accuracy.

### L1-FIXTURE-PIPE-CORE assigned — 25 September 2026

After accepting L1-WRITE-IDEMPOTENCY-CORE, root reviewed the queue/parser/storage seams and selected a bounded local queue-to-fixture processor as the next ready slice. [L1-FIXTURE-PIPE-CORE](assignments/L1-FIXTURE-PIPE-CORE.md) is assigned on `work/L1-FIXTURE-PIPE-CORE` in `.codex-build/worktrees/l1-fixture-pipe-core`. It accepts only an already-claimed synthetic moderator-submission job, resolves exact URLs from an injected local catalog, and persists schema 2.0 revisions from authored fixture text and metadata with explicit evidence spans and geometry roles. It receives rather than claims jobs because the shared `claimDueJob` query has no dataset/job-kind filter. Parser-only source creation time, provider status, and report type remain transient; the closed schema has no approved fields, and `created_at` is never mapped to incident time or publication time. Geometry must be explicitly specified and 3D input is rejected rather than altered. Empty input completes without records or an all-clear result. The task does not add live acquisition, API wiring, a dependency, migration, source activation, auth or publication.

### L1-FIXTURE-PIPE-CORE accepted — 25 September 2026

Root reviewed the task branch implementation commit `01c9bac465b3839f16b7efe50b20fadab7159d3d` (`feat(L1-FIXTURE-PIPE-CORE): process synthetic moderator fixtures`) and handoff commit `ea117d7df326d3e1b383a00fd8ece1cad5095407` (`docs(L1-FIXTURE-PIPE-CORE): record implementation handoff`), then merged them as `e5b164796b5d2bd35903d5555a69ab7bcd683d81` (`merge: accept synthetic fixture pipeline`). The new Worker processor consumes only already-leased synthetic moderator submissions, resolves exact URLs from an injected in-memory fixture catalog, constructs closed schema 2.0 unreviewed revisions from explicit manifest data, and persists deterministic text chunks, exact supporting spans, and only manifest-mapped 2D CRS84 geometry. It never claims or fetches jobs, does not promote parser-only metadata into schema fields, and completes after writes. Empty input has an explicit zero-row result; stale and uncertain acknowledgements avoid a second queue transition. No migration, API, dependency, route, or live source was added.

In WSL Ubuntu-26.04 with Node.js `v24.21.0` and npm `11.19.0`, the final Worker suite passed 62/62, `npm run typecheck` and `npm run build` passed, and `git diff --check` passed. The new PGlite integration passed 1/1 under `SET ROLE waspada_l1_pipeline`; root also ran all eight database test files individually and each passed. The aggregate `npm run db:test` exited 1 without a test summary. Aggregate `npm test` passed web 5/5 and Worker 61/61 in its run, then the DB runner exited 1 after its first suite without a failure summary. This remains an unresolved aggregate test-runner limitation, not a reported pass. PGlite uses one local session and does not establish hosted Neon concurrency; source/data rights remain pending.

### DB-TEST-RUNNER-CORE selected — 25 September 2026

The next-slice audit found that real-source connectors, model evaluation, publication services and release work remain gated by pending source/data rights or human-adjudicated cases. The clearer immediate local prerequisite is the aggregate test-runner failure: `npm run db:test` and `npm test` stop without a DB failure summary, while all eight DB test files pass individually. Root assigned [DB-TEST-RUNNER-CORE](assignments/DB-TEST-RUNNER-CORE.md) to reproduce and diagnose that issue, then repair it within the DB test script or test harness only. The task must preserve all tests, add no dependencies, and pass the aggregate DB and full workspace suites in WSL. It uses branch `work/DB-TEST-RUNNER-CORE` and worktree `.codex-build/worktrees/db-test-runner-core`; implementation and review remain pending.

### DB-TEST-RUNNER-CORE investigated — 25 September 2026

Root reviewed `work/DB-TEST-RUNNER-CORE`, based on clean commit `3b9930a874250f2bcb9e1eabcb2efb5bc17e3c5c`. In WSL Ubuntu-26.04 with Node.js `v24.21.0` and npm `11.19.0`, the agent ran `npm run db:test` alone and it passed 61/61 across all eight database files, each reported once. A separate `npm test` passed 140/140 (web 5, Worker 62, database 61, evaluation 12). `npm run typecheck`, `npm run build` (Vite production and Wrangler deploy dry-run), and `git diff --check` also passed. The previous exit 1 was not reproducible and had no saved failure summary or other root-cause evidence. Default per-file parallelism was observed, but the isolated passing runs do not establish that concurrency caused the old failure. No script or test change was justified. The only branch change was the assignment handoff at commit `e5f03a8b228a8d1104fa2ae2ceb4f24cbc8bc9d0`; it was fast-forwarded to `main`. Current verification is accepted as passing, but the historical failure remains unexplained and can be reopened if a failing environment/log is available.

### L3-LEDGER-CORE selected — 25 September 2026

After the aggregate DB runner passed in two isolated WSL runs, root audited the remaining local dependencies. The database schema already contains bounded investigation requests and checkpoints, but no typed Layer 3 persistence boundary exists. It also ties every checkpoint to the request's original context, preventing the documented L2 refresh loop from preserving a case while using a newer context. Root recorded [ADR-014](decisions/ADR-014-l3-investigation-ledger.md) to retain schema 2.0 records while adding same-dataset/candidate checkpoint refresh, stable idempotent action reservations, durable counter reconciliation, append-only snapshot history and a least-privilege NOLOGIN role. This design is limited to persistence: it adds no model/tool invocation, live-source enablement, public API, or publication authority. The local implementation is assigned in [L3-LEDGER-CORE](assignments/L3-LEDGER-CORE.md) on `work/L3-LEDGER-CORE` and `.codex-build/worktrees/l3-ledger-core`.

### L3-LEDGER-CORE accepted — 26 September 2026

Root reviewed `work/L3-LEDGER-CORE` and fast-forwarded it to `main` at handoff `32d4678`. Implementation commit `ed0f307` adds migration 008, a typed transactional repository, six synthetic PGlite behavior tests, and exact-grant assertions. It opens cases only from insufficient persisted contexts with matching dataset/candidate/trace, keeps the initial request context while allowing same-candidate refreshed checkpoint contexts, reserves one action before invocation, reconciles bounded usage exactly once, charges interrupted actions at their reservation, and makes checkpoint history append-only. The coordinator role is `NOLOGIN`; its successful operations and denials are tested under `SET ROLE`. The schema 2.0 JSON and public API remain unchanged; no model/tool adapter, Worker route, source acquisition, or publication capability was added.

Root independently ran the focused ledger suite (6/6) and migration/privilege suite (8/8) in WSL Ubuntu-26.04 with Node.js `v24.21.0` and npm `11.19.0`. Typecheck, Vite/Wrangler build, and diff checks pass per the branch handoff. The implementer reports all nine DB files pass in separate processes (68 tests total). However, two isolated `npm run db:test` runs and `npm test` exit 1 without DB assertion diagnostics; a single-worker aggregate also exited without a final summary. The full aggregate is not recorded as passing. PGlite does not verify hosted PostgreSQL concurrency or Neon behavior.

### DB-TEST-RUNNER-ISOLATION assigned — 26 September 2026

Because the aggregate issue reappeared after the DB suite grew from eight files to nine, root assigned [DB-TEST-RUNNER-ISOLATION](assignments/DB-TEST-RUNNER-ISOLATION.md) on `work/DB-TEST-RUNNER-ISOLATION`. The implementer must first reproduce the current behavior, then may add a built-in, sequential per-file process launcher if needed. It must preserve every test, print each result, and return failure for any failed or interrupted child. No dependencies, test changes, or suppressed failures are authorized.

### DB-TEST-RUNNER-ISOLATION accepted — 26 September 2026

Root reviewed and fast-forwarded the task branch to `main` at handoff `805ccd48d8a72662b38a1b49648d6461be8b0781`, from implementation commit `cac88f9eadf0c8bb4d2ce0e1898dda916d24bd4e`. The database test script now launches every discovered `*.test.ts` file in stable lexical order in a separate native Node/tsx process. Child output is inherited, failed files do not prevent later tests from running, and any spawn error, signal, non-zero exit, or parent interruption yields an aggregate failure. Parent interruption stops scheduling further files. No dependencies, lockfile, assertion, migration, product code, CI or deployment settings changed.

Root independently verified the merged task in WSL Ubuntu-26.04 with Node.js `v24.21.0` and npm `11.19.0`: `npm run db:test` passed 9/9 files and 68/68 tests; `npm test` passed 147/147 (web 5, Worker 62, DB 68, evaluation 12); `npm run typecheck` passed; `npm run build` passed Vite production build and Wrangler deploy dry-run; and `git diff 8184cac...HEAD --check` passed with explicit WSL Git directory/worktree paths. The implementer also passed these checks on its task branch and committed a separate detailed handoff.

The original default aggregate exit (status 1 after 29.45 seconds with no assertion output) and one isolated `migrations.test.ts` silent exit remain unexplained. Its immediate retry and all final sequential runs passed. The launcher intentionally has no retry: any recurrence is printed and returned as a non-zero aggregate, preserving the signal rather than masking it. The additional WSL-local Node installation through nvm changed no repository files; final checks used the pre-existing documented Node v24.21.0 path.

### L1-EVIDENCE-RELATION-ALIGN-CORE selected — 26 September 2026

The contract audit found that schema 2.0 `EvidenceRef` and the L2 model contract allow `updates`, while the database constraint and DB relation type allow only `supports`, `contradicts`, and `context`. Root recorded [ADR-016](decisions/ADR-016-evidence-reference-relation-alignment.md) and assigned [L1-EVIDENCE-RELATION-ALIGN-CORE](assignments/L1-EVIDENCE-RELATION-ALIGN-CORE.md) as the next local prerequisite. The task adds only a forward migration and typed-storage/test coverage so all four existing relation values round-trip exactly; publication policy remains fail-closed for `updates` as claim support. This prerequisite unblocks the already planned [L2-CONTEXT-PERSIST-CORE](assignments/L2-CONTEXT-PERSIST-CORE.md), which stores only the canonical refs-only schema 2.0 grounding-context record and normalized links. No API/schema change, live data, model call, or hosted service is involved. The user-requested `docs/api/openapi.yaml` restoration from `main` was applied and verified clean.

### L1-EVIDENCE-RELATION-ALIGN-CORE accepted — 26 September 2026

Root reviewed `work/L1-EVIDENCE-RELATION-ALIGN-CORE` and merged it to `main` at `7dd8ea7c5d540a56a1b4436f456b5f678ff6e9a1`. Implementation commit `34ecb04665f019f4ab1f8e266f505f9f80b4f9e5` adds migration 009, aligns `apps/db/src/ports.ts` with schema 2.0, and tests all four relation values. The migration preserves existing evidence rows, verifies transactional rollback and reapplication, rejects unrelated values, and leaves role state unchanged. `updates` round-trips through the L1 writer and L2 retrieval reader without becoming claim support in L4. The separate handoff is `9b8f20ad3f318fa3cbacb029ac62ec262cbb31b8`.

Root independently passed WSL Ubuntu-26.04 checks with Node.js `v24.21.0` and npm `11.19.0`: `npm run db:test` passed 9/9 files (70 tests); `npm test` passed 149/149 (web 5, Worker 62, DB 70, casebook 12); `npm run typecheck`, `npm run build` (Vite and Wrangler dry-run), and `git diff --check` passed. No dependency, schema 2.0 contract, OpenAPI, API, Worker, L4 policy, role, grant, or provider change was made. PGlite does not verify hosted Neon behavior or concurrency. The next local slice is [L2-CONTEXT-PERSIST-CORE](assignments/L2-CONTEXT-PERSIST-CORE.md), whose writer migration is numbered 010 after L1 migration 009.

### L2-CONTEXT-PERSIST-CORE assigned — 26 September 2026

With L1 relation alignment accepted, root assigned [L2-CONTEXT-PERSIST-CORE](assignments/L2-CONTEXT-PERSIST-CORE.md) on `work/L2-CONTEXT-PERSIST-CORE` in `.codex-build/worktrees/l2-context-persist-core`, based on `main` at `53ae205`. Its forward role migration is numbered 010 after L1 migration 009. The task persists only the canonical schema 2.0 refs-only `GroundingContext` plus normalized evidence/event/decision links through a transactional create-or-verify repository and a distinct NOLOGIN writer role. `sufficient` is stored exactly as supplied and remains unevaluated. Synthetic fixtures only; no Worker/API wiring, live source, model call, external service, or deployment.

### L2-CONTEXT-PERSIST-CORE accepted — 26 September 2026

Root reviewed and merged the task branch to `main` at `6effd54c9ec3b8a794f4410925f14ce1cb3fdb55`. The work was committed by the implementation agent in separate, auditable commits: `9c5f58228990b828a3fc643f0e16fe718cc14eec` added the typed canonical grounding-context writer, migration 010, and synthetic integration tests; `075ddd384a2e0eb641df621fd53bf8e9cb821898` recorded the handoff. Root review found and sent back a schema-parity issue; follow-up commit `25454b5078765161eb7ff232bebd4860f71b0378` corrected JSON Schema string bounds to count Unicode code points, and `ab092bd2234baca9ccd42418307bef939eb128d8` documented the correction and checks. Duplicate `revision_states` entries remain intact as allowed by schema 2.0; normalized evidence, candidate-event, and decision links reject duplicate link identities.

The persisted contract excludes retrieved excerpt text and expanded prompt/reasoning content. Same-dataset evidence, event versions, and prior decisions must already exist. One required SQL transaction inserts the immutable parent and all links, and identical retries verify both the row and full link sets. The separate `waspada_l2_grounding_writer` is `NOLOGIN NOINHERIT`; synthetic tests exercise create/replay under `SET ROLE`, exact column/table privileges, denied unrelated reads/writes, rollback and migration reapplication. `sufficient` is preserved unchanged and is not an assessment or publication decision.

Root independently verified the integrated tree in WSL Ubuntu-26.04 with Node.js `v24.21.0` / npm `11.19.0`: `npm run db:test` passed all 10 files (77 tests); `npm test` passed 156/156 (web 5, Worker 62, DB 77, evaluation casebook 12); `npm run typecheck`, `npm run build` (Vite production build and Wrangler deploy dry-run), and `git diff d45f137..HEAD --check` passed. No deployment occurred, dependencies changed, or cloud resources were created. PGlite does not establish hosted Neon/PostgreSQL role setup or concurrent transaction behavior. Worker wiring, source rights and data, model use, human-labelled evaluation, retrieval quality and sufficiency assessment remain out of scope or gated.

### L2-CONTEXT-BRIDGE-CORE selected — 26 September 2026

After accepting the canonical L2 persistence writer, root selected and assigned the next rights-independent integration seam: validate an untrusted Worker `ReasoningRequest`, map its schema 2.0 fields to the refs-only DB record, call the injected writer once, and return both the intact excerpt-bearing in-memory request and the persisted record. The bridge must preserve the supplied `sufficient` value without assessment, and must not invoke retrieval, a model, tools, L3, a route or publication. The scope is synthetic-only and adds no contracts or dependencies. Root created `work/L2-CONTEXT-BRIDGE-CORE` in `.codex-build/worktrees/l2-context-bridge-core`, based on planning commit `3c2d2f0`; implementation and root review are pending. Source rights and human adjudication remain pending.

### L2-CONTEXT-BRIDGE-CORE accepted — 26 September 2026

Root reviewed and merged the branch at `6ced3ea`. Agent commits are `4cebaa2` (`feat(L2-CONTEXT-BRIDGE-CORE): bridge reasoning context persistence`) and `32b4460` (`docs(L2-CONTEXT-BRIDGE-CORE): record implementation handoff`). The injected adapter validates an unknown request, projects all schema 2.0 storage fields without excerpts or provenance, calls the repository once, and returns the validated in-memory request beside the repository result. It leaves `sufficient` uninterpreted and propagates typed repository errors unchanged. No contracts, migrations, dependencies, routes, models, live sources or hosted resources changed.

The agent passed WSL checks for focused tests (4/4), `npm test` (160/160), typecheck, build and staged/unstaged diff checks. Root independently passed the focused bridge test (4/4), the 160-test workspace suite, typecheck, build and `git diff --check` against the integrated diff. Build included only the Vite production build and Wrangler dry-run. Hosted DB behavior and source-backed quality remain unverified; rights-cleared evaluation remains pending.

### OBS-01-API-TELEMETRY-CORE assigned — 26 September 2026

The accepted Worker API measures route category, response status, and duration but currently sends each observation to a no-op sink. Root selected this as the next rights-independent Layer 5 slice. The assigned implementation keeps a typed injected sink, emits only a fixed event name plus route/status/duration through the Worker structured console logger, and ensures logging failures cannot change API responses. Wrangler will sample at 1% and disable invocation logs to avoid storing request URLs/query strings. Cloudflare currently documents Workers Logs on Free with 200,000 log events/day and three-day retention ([Workers Logs](https://developers.cloudflare.com/workers/observability/logs/workers-logs/), [pricing](https://developers.cloudflare.com/workers/platform/pricing/)); no external resource will be enabled by this local change and no deployment is part of the task. Implementation is assigned on `work/OBS-01-API-TELEMETRY-CORE` in `.codex-build/worktrees/obs-01-api-telemetry-core`. Human-labeled quality metrics and source-health monitoring remain later OBS-01 work.

### OBS-01-API-TELEMETRY-CORE accepted — 26 September 2026

Root reviewed `work/OBS-01-API-TELEMETRY-CORE` and fast-forwarded it to `main` at `8135a90`. The implementation commit is `69873247d21d17bf5b1839ea104b2e928589acbd` (`feat(OBS-01-API-TELEMETRY-CORE): add safe sampled API telemetry`); the separately committed handoff is `8135a907e95fb718e16334aa416f6d49437e75a3` (`docs(OBS-01-API-TELEMETRY-CORE): record implementation handoff`). It adds a typed injectable sink to the read API and an allowlisted structured Worker logger for a fixed event name, route category, status and duration. Sink failures cannot alter responses. Wrangler config samples at 1% and disables invocation logs that can contain request URLs.

The root independently verified the integrated tree in WSL Ubuntu-26.04 using Node.js `v24.21.0` and npm `11.19.0`: `npm test` passed 163/163 (web 5, Worker 69, DB 77, casebook 12), `npm run typecheck`, `npm run build` (Vite and Wrangler dry-run) and `git diff --check` passed. Wrangler accepted the configuration in dry-run only. No deployment, account/resource change, API contract, database migration, dependency or lockfile change occurred. The request-telemetry slice does not provide full-stack quality, model-cost, source-health, dashboard, alert or human-feedback monitoring.

Cloudflare's current documentation lists Workers Logs on Free at 200,000 log events/day with three-day retention and confirms that explicit observability configuration collects custom logs: [Workers Logs](https://developers.cloudflare.com/workers/observability/logs/workers-logs/), [Workers pricing](https://developers.cloudflare.com/workers/platform/pricing/). The 1% sampling is a configured volume control; actual remote collection and account usage remain unverified because no deployment occurred.

### OBS-01-L2-RETRIEVAL-TELEMETRY-CORE assigned — 26 September 2026

After accepting safe operational telemetry for the synthetic API, root selected the next ready OBS-01 slice at the existing Layer 2 retrieval seam. The typed L5 telemetry sink will gain one closed L2 event shape for retrieval outcome, elapsed duration, candidate/row counts, truncation flags and semantic-status enum. The adapter will report these bounded metrics while preserving its exact query/result/error pass-through. It must never log terms, IDs, geography, source data, excerpts, origins or vectors; telemetry remains injected and defaults to no-op because the Worker has no database composition path. RAG-CORE and RAG-ACCESS-01 are accepted and the work uses synthetic fixtures only. The implementation branch/worktree is `work/OBS-01-L2-RETRIEVAL-TELEMETRY-CORE` / `.codex-build/worktrees/obs-01-l2-retrieval-telemetry-core`. No API, SQL, schema, model, source or cloud change is authorized.

### OBS-01-L2-RETRIEVAL-TELEMETRY-CORE accepted — 26 September 2026

Root reviewed the assigned branch and fast-forwarded it to `main` at `12509e3`. Luna Max committed implementation `4850da0e0bc3c1967d2c16db1c276cc6640a3339` (`feat(OBS-01-L2-RETRIEVAL-TELEMETRY-CORE): add privacy-safe L2 retrieval telemetry`) and handoff `12509e3f9f93c1c97cb8d662a4a454c021abea12` (`docs(OBS-01-L2-RETRIEVAL-TELEMETRY-CORE): record implementation handoff`). The retrieval adapter emits only bounded success/error metrics through an injected, fail-open sink; query, evidence content and identifiers are excluded, and the existing result/error is preserved exactly. The console serializer uses explicit allowlists. Its default remains no-op because there is no Worker/database composition path. No API contract, SQL, schema, migration, dependency, source connection, cloud account or deployment changed.

The agent passed 11/11 focused retrieval/API tests, typecheck, build and diff checks in WSL Ubuntu-26.04. Root independently verified `npm test` (166/166: web 5, Worker 72, DB 77 across 10 files, casebook 12), `npm run typecheck`, `npm run build` (Vite and Wrangler dry-run), and `git diff d888d92..HEAD --check` with Node.js `v24.21.0` / npm `11.19.0`. This verifies the local synthetic modules only; it does not establish telemetry delivery, hosted Neon behavior, retrieval accuracy or model behavior.

### OBS-01-L3-LEDGER-TELEMETRY-CORE assigned — 26 September 2026

With API and L2 retrieval operational telemetry accepted, root assigned the next dependency-safe L5 slice around existing Layer 3 ledger writes. The Worker-side decorator will report only bounded operation, lifecycle/stop-reason, elapsed-time and consumed-budget summaries through an injected sink; reads remain uninstrumented, and case/question/tool/model identifiers and exception details are excluded. Results/errors remain unchanged, sink failures fail open, and the sink remains no-op unless a caller injects it. No DB repository, schema, coordinator, source, model, API or runtime composition change is authorized. The assignment is `docs/assignments/OBS-01-L3-LEDGER-TELEMETRY-CORE.md` on `work/OBS-01-L3-LEDGER-TELEMETRY-CORE` / `.codex-build/worktrees/obs-01-l3-ledger-telemetry-core`.

### OBS-01-L3-LEDGER-TELEMETRY-CORE accepted — 26 September 2026

Root reviewed and integrated `work/OBS-01-L3-LEDGER-TELEMETRY-CORE` on `main` at `408de10dfeb010d71d3fc351fea633bbdd87213d`. The separate implementation commit is `e6f39562be8dafd599ebac631d0e042793f9e3e9`; the handoff records scope and checks. The new Worker decorator emits bounded Layer 3 write-transition summaries through the typed Layer 5 sink, preserving exact repository results/errors and failing open if telemetry throws. It omits investigation IDs, questions, source/action/model identifiers and error details; read methods remain uninstrumented. The sink defaults to no-op, with no Worker/database runtime composition added.

The implementation agent passed focused API/L2/L3 telemetry tests (17/17), typecheck, build and diff checks in WSL Ubuntu-26.04. Root independently passed `npm test` (172/172: web 5, Worker 78, DB 77, casebook 12), `npm run typecheck`, `npm run build` (Vite production build and Wrangler dry-run), and `git diff b874999..HEAD --check`. These local synthetic checks do not verify hosted database behavior or remote Cloudflare log collection. No contract, schema, migration, dependency, source, model, external account or deployment changed.

### RAG-CONTEXT-ASSEMBLY-CORE assigned — 26 September 2026

Root assigned a bounded L2 integration seam after reviewing the existing retrieval and reasoning contracts. Retrieval returns a bounded excerpt, while the reasoner requires exact text whose code-point length matches the cited stored span. The task re-reads only the selected full evidence spans under the existing least-privilege L2 role, validates exact dataset/reference identity, and assembles a strict version 2.0 reasoning request with provenance and contradictions intact. The caller supplies sufficiency and related context explicitly; the adapter does not assess it. Truncated, oversized, stale or mismatched evidence must not become a reasoning request. Synthetic-only; no model, L3, source, API, publication, contract, grant or external-service operation. See [assignment](assignments/RAG-CONTEXT-ASSEMBLY-CORE.md).

### RAG-CONTEXT-ASSEMBLY-CORE accepted — 26 September 2026

Root reviewed and fast-forwarded `work/RAG-CONTEXT-ASSEMBLY-CORE` on `main` at `f4ca0dc` (`6a2ee3b` implementation, `f4ca0dc` handoff). The DB reader rechecks persisted dataset/candidate/reference/revision/hash/offset/unit/relation/status identity under the existing L2 role and returns only the exact span. The Worker assembler requires complete retrieval and a single candidate identity, rehydrates only explicitly selected references, preserves source times, relation, revision state, provenance, conflicts and caller-supplied sufficiency, and validates schema 2.0 before returning. No model, L3/L4, API, source, publication, migration, grant, dependency or external-service operation was added. The requested OpenAPI restore from `main` was applied; no OpenAPI diff remained.

Root independently ran WSL Ubuntu-26.04 with Node.js `v24.21.0` and npm `11.19.0`: focused DB/Worker tests passed 14/14 and 6/6, `npm run db:test` passed 78/78 across 10 files, `npm test` passed 179/179 (web 5, Worker 84, DB 78, casebook 12), and typecheck, Vite/Wrangler build and diff checks passed. These checks use synthetic data and PGlite; hosted Neon grants/concurrency, Worker database wiring, and provider/model behavior remain unverified.

### L2-DIRECT-REASONING-CORE assigned — 26 September 2026

After accepting exact-span grounding, root assigned a local L2 composition slice. It must persist a validated refs-only context first, return `investigation_required` without a reasoner call when the caller's explicit `sufficient` value is false, and invoke the injected `ModelCapabilityAdapter.reason` once only when it is true. Typed provider-unavailable/errors remain visible. A closed, opt-in L5 telemetry record may report outcome and duration only. Synthetic provider doubles are test-only; no real provider, L3 coordinator, API route or publication path is included. See [assignment](assignments/L2-DIRECT-REASONING-CORE.md).

### L2-DIRECT-REASONING-CORE accepted — 26 September 2026

Root reviewed and fast-forwarded `work/L2-DIRECT-REASONING-CORE` on `main` at `c3e2005` (`42afe02` implementation, `c3e2005` handoff). The service validates and persists the refs-only context before branching; caller-insufficient context returns `investigation_required` without a reasoner call, while sufficient context reaches the injected reasoner once and returns its typed outcome unchanged. Validation/persistence errors retain object identity. L2 telemetry emits a closed outcome and finite duration only, defaults to no-op, and fails open.

Root independently ran WSL Ubuntu-26.04 with Node.js `v24.21.0` and npm `11.19.0`: focused direct-reasoning tests passed 9/9; `npm test` passed 188/188 (web 5, Worker 93, DB 78 across 10 files, evaluation 12); typecheck, Vite/Wrangler build and diff checks passed. No provider, coordinator, route, publication, or deployment was added. Tests use synthetic requests and scripted provider doubles only; hosted provider/database runtime remains unverified.

### L3-INSUFFICIENT-CONTEXT-ENTRY-CORE assigned — 26 September 2026

Root assigned the first L2-to-L3 integration slice after accepting direct reasoning. It will create or idempotently replay a durable ledger case only from the typed `investigation_required` outcome and its exact persisted insufficient context. Question records use bounded stable labels rather than source/conflict text; ambiguous candidate-event matches or question limits route to review. The existing ledger enforces budget maxima and exact context linkage. The assignment adds no tool execution, planner/model call, source fetch, route, publication or external service. See [assignment](assignments/L3-INSUFFICIENT-CONTEXT-ENTRY-CORE.md).

### L3-INSUFFICIENT-CONTEXT-ENTRY-CORE accepted — 26 September 2026

Root reviewed and fast-forwarded the assigned branch on `main` at handoff `c8c16c5` (`9bf5407` implementation, `c8c16c5` handoff). The entry adapter accepts only the typed L2 `investigation_required` result, verifies the agreed persisted/request identity and insufficient state, and calls the existing bounded ledger with explicit caller ID, time, policy and budget. It derives question strings solely from positions as `missing_field_N` and `conflict_N`; no raw evidence/conflict strings are copied. Zero/more-than-20 questions and multiple event matches return `review_required`; zero/one event matches map to null/exact persisted pair. It preserves the exact ledger checkpoint and error objects. No planner, tool, model, source fetch, route, publication, migration or runtime wiring was added.

Root independently ran WSL Ubuntu-26.04 with native Node.js `v24.21.0` and npm `11.19.0`: focused entry test passed 8/8; `npm test` passed 196/196 (web 5, Worker 101, DB 78 across 10 files, evaluation 12); `npm run typecheck`, `npm run build` (Vite and Wrangler 4.137.0 dry-run), and `git diff main...HEAD --check` passed. Tests use synthetic schema 2.0 contexts and an in-memory ledger fake; hosted PostgreSQL/Neon and application/database runtime composition remain unverified. See [assignment](assignments/L3-INSUFFICIENT-CONTEXT-ENTRY-CORE.md) and [handoff](assignments/L3-INSUFFICIENT-CONTEXT-ENTRY-CORE-HANDOFF.md).

### L3-SINGLE-STEP-EXECUTOR-CORE assigned — 26 September 2026

After accepting the L2-to-L3 case entry, root accepted ADR-017 and assigned a single-action execution boundary. The service will validate a proposal against a trusted injected action registry, reserve the worst-case tool budget in the existing ledger before start, invoke only when the ledger grants `mayInvoke`, and reconcile one closed bounded result. Replays will return their ledger state without invoking again. The assignment uses synthetic handlers and adds no planner loop, live tool/source, route, scheduler or runtime wiring. See [ADR-017](decisions/ADR-017-l3-single-step-execution.md) and [assignment](assignments/L3-SINGLE-STEP-EXECUTOR-CORE.md).

### L3-SINGLE-STEP-EXECUTOR-CORE accepted — 26 September 2026

Root reviewed and fast-forwarded `work/L3-SINGLE-STEP-EXECUTOR-CORE` to `main` at handoff `22b1f10` (`4d52744` initial implementation, `6543d16` replay-order fix, `35f049b` aggregate-input bound). The executor validates one strict proposal against a trusted injected registry, then calls the existing ledger before current-state denials so exact reservation replays continue to win after closure or later activity. Only a first `startAction` result with `mayInvoke: true` invokes the handler. Each call executes at most one handler, reconciles one closed bounded receipt, and returns only status plus stable reference IDs. The proposal clone is capped at 32,768 UTF-16 code units across string values and plain-object keys, with 16,384 per string and 128 per key. Only typed expected denials become review results; unrelated ledger errors retain identity. No application action is registered, and no planner, source/provider, route, publication or runtime wiring was added.


### API-GEOMETRY-CORE accepted — 26 September 2026

Luna Max implemented the assignment on `work/API-GEOMETRY-CORE` in `.codex-build/worktrees/api-geometry-core`. Implementation commit `ac30df25d989a79219fa098a4e5f2e8073b24e8b` (`feat(API-GEOMETRY-CORE): project supported public geometry`) and handoff commit `c0d6ade73375c6d0b0ea94d0e2c55cc68462f860` (`docs(API-GEOMETRY-CORE): record projection handoff`) were fast-forwarded to `main` after root review. The implementation adds a pure L4 projection for source-linked event geometry, public event detail, and GeoJSON. It requires exact event/claim geometry references and matching `supports` evidence spans, validates CRS84 role-compatible 2D structure and finite coordinate bounds, applies deterministic output caps, and emits only explicit public allowlists. It does not transform coordinates, infer spatial regions, read the database, expose a route, or change the OpenAPI contract. The [assignment](assignments/API-GEOMETRY-CORE.md) and [handoff](assignments/API-GEOMETRY-CORE-HANDOFF.md) record the exact paths and contract.


### API-DETAIL-HISTORY-ROUTES-CORE accepted — 26 September 2026

Luna Max implemented the assigned demo-only routes on `work/API-DETAIL-HISTORY-ROUTES-CORE` in `.codex-build/worktrees/api-detail-history-routes-core`. Implementation commit `c66bdc140a7365556e6173e1644c0a3e06267f97` (`feat(API-DETAIL-HISTORY-ROUTES-CORE): add synthetic detail and history routes`) and handoff commit `48c7f736b203f12caac854db0fe75d25c6bc9f00` (`docs(API-DETAIL-HISTORY-ROUTES-CORE): record implementation handoff`) were fast-forwarded to `main` after root review. Detail returns only the existing fictional EventView fixture fields plus an empty geometry list; history returns only one synthetic fixture version with the fixture's existing `published_at` and a clear synthetic summary. The routes validate identifiers and bounded pagination, return safe 400/404/500 errors, reject mutations, and use only the fixed `events` telemetry category. Explicit non-demo mode returns 503 before fixture access. OpenAPI and public DTO schemas remain unchanged.

The agent and root independently ran WSL Ubuntu-26.04 with Node.js `v24.21.0` and npm `11.19.0`: focused API tests passed 13/13; `npm test` passed 226/226 (web 5, Worker 131, DB 78 across 10 isolated files, evaluation casebook 12); `npm run typecheck`; `npm run build` (Vite production build and Wrangler `4.137.0` dry-run); and WSL diff checks all passed. No database read, GeoJSON route, source adapter, publication/auth behavior, dependency, migration, runtime binding, external service, or deployment was added. The fixtures are authored and fictional: these checks do not establish source rights, factual support, real event history, hosted DB behavior or safety. The worktree was clean after removing its temporary dependency link. See the [assignment and handoff](assignments/API-DETAIL-HISTORY-ROUTES-CORE.md).

### UI-API-DETAIL-HISTORY-CORE accepted — 26 September 2026

Root reviewed and fast-forwarded `work/UI-API-DETAIL-HISTORY-CORE` to local `main` at handoff `e53caf5` (`113e032d2b39f088dfcf3edb1c2b9630395c4d4` implementation; `e53caf5b5220d35f0eb52b198485210918e51e24` handoff). The existing API detail page now fetches `EventDetail` and `HistoryPage` from the synthetic GET routes using encoded event IDs. Detail and history requests load independently with loading, not-found, unavailable and retry states; cancellation plus event-ID keyed state prevents stale route responses from replacing the selected record. The page shows only returned fixture data, keeps empty evidence/history explicit, preserves the global demo label and uses the existing no-geometry fallback. OpenAPI, CSS, API behavior and domain contracts are unchanged.

The agent and root independently passed focused web/API tests 9/9. Root independently ran WSL Ubuntu-26.04 with Node.js `v24.21.0` and npm `11.19.0`: the full suite passed 230 tests (web 9, Worker 131, DB 78 across 10 isolated files, casebook 12), typecheck passed, and `npm run build` passed with the Vite production build and Wrangler `4.137.0` dry-run. WSL diff checks passed with explicit worktree Git paths. The agent reviewed screenshots at desktop 1440×900 and mobile 390×844; no mobile horizontal overflow or design-direction changes were found. No live source, database reader, GeoJSON route, dependency, migration, external service or deployment was added. Synthetic data does not establish real evidence, currentness, safety or source rights. Temporary dependencies and generated artifacts were removed. See the [assignment and handoff](assignments/UI-API-DETAIL-HISTORY-CORE.md).

### UI-02-LOCAL-PREFERENCES-CORE accepted — 26 September 2026

Root reviewed and fast-forwarded `work/UI-02-LOCAL-PREFERENCES-CORE` to local `main` at handoff `b469a020dab12182886d6939efb768d4ddc64bc0`. The implementation commit is `be4c34dd762fcb6b0f757b7e0ceb0204881c4934` (`feat(UI-02-LOCAL-PREFERENCES-CORE): add browser-local preferences`); the handoff commit is `b469a020dab12182886d6939efb768d4ddc64bc0` (`docs(UI-02-LOCAL-PREFERENCES-CORE): record implementation handoff`). Changed paths: `apps/web/package.json`, `apps/web/src/App.tsx`, new `Preferences.tsx`, new `preferences-store.ts`, `apps/web/src/styles.css`, new `apps/web/test/preferences.test.tsx`, and the task handoff.

The public `#ringkasan-saya` view lets visitors edit places, services, institutions, audiences, and the ten existing categories. Values follow current contract limits, and users explicitly save or clear a versioned browser-local record. Malformed JSON and storage read/write/clear errors are recoverable and do not silently replace unreadable content. The screen explains local storage and that briefing/update matching is not connected. It makes no network call or synthetic relevance/safety claim.

The agent and root passed focused web tests (20/20). Root independently verified in WSL Ubuntu-26.04 with Node.js `v24.21.0` and npm `11.19.0`: `npm test` passed 241/241 (web 20, Worker 131, DB 78 across 10 isolated files, casebook 12); `npm run typecheck`, `npm run build` (Vite production build and Wrangler dry-run), and `git diff --check` passed. Root reviewed headless screenshots at desktop 1440×900 and mobile 390×844. The page had no horizontal overflow; the agent also measured 390px and 320px widths. No API contract, dependency, server, database, migration, configuration, external service, or deployment changed. Briefing and updates remain unimplemented. The agent's temporary dependency link was removed. See the [assignment and handoff](assignments/UI-02-LOCAL-PREFERENCES-CORE.md).

### ADR-018 and API-GEOJSON-ROUTE-CORE assigned — 26 September 2026

Following the user's instruction to choose an application query envelope, root recorded the inclusive CRS84 limit `west=106.32, south=-6.40, east=106.98, north=-5.16` in ADR-018 and clarified the existing optional OpenAPI `bbox` description. These values round outward the geographic extent published in Jakarta's 2025–2029 regional-plan materials ([RPJMD academic draft](https://dprd-dkijakartaprov.go.id/wp-content/uploads/2025/01/Ranhir-Naskah-Akademik-RPJMD-2025-2029_21-Mei_compressed.pdf), [strategic environmental review](https://lingkunganhidup.jakarta.go.id/files/klhs/Lap_KLHS_RPJMD_Prov_DKI_Jkt_Th_2025-2039_compre.pdf)). The envelope limits API queries; it is not a legal boundary, event geometry, or warning zone. No boundary dataset was acquired. Root assigned `API-GEOJSON-ROUTE-CORE` on `work/API-GEOJSON-ROUTE-CORE` in the managed worktree `C:\Users\perry\.codex\worktrees\api-geojson-route-core\RPL` to add the demo-only read route and strict bbox validation. Current fixtures have no supported geometry, so the demo result remains an empty collection. No live reader, database, source, or external resource is included.

### API-GEOJSON-ROUTE-CORE accepted — 26 September 2026

Luna Max implemented the bounded synthetic route on `work/API-GEOJSON-ROUTE-CORE` in `C:\Users\perry\.codex\worktrees\api-geojson-route-core\RPL`. Implementation commit `84b460703e6d24161dc8d0a9caaed26c5fcd972a` (`feat(API-GEOJSON-ROUTE-CORE): add synthetic GeoJSON route`) and handoff commit `29aa6b4ba0bb0f923247750a52cac5a92a5344dd` (`docs(API-GEOJSON-ROUTE-CORE): record implementation handoff`) were fast-forwarded to `main` after root review. The route serves `GET /api/v1/events.geojson` as `application/geo+json`, validates optional CRS84 bounds against the inclusive ADR-018 envelope and validates only the existing category, lifecycle and freshness enums. It rejects unknown filters, preserves 405 and non-demo 503 behavior, and records only the fixed `events` telemetry category. Current demo fixtures have no source-supported geometry, so the response is an exact empty FeatureCollection; filters are validated but cannot change the empty result. No database reader, live spatial filtering, contract, migration, dependency, binding, or external service was added.

Root independently verified in WSL Ubuntu-26.04 with Node.js `v24.21.0` and npm `11.19.0`: focused Worker API tests passed 19/19; the full `npm test` exited successfully, including all 10 isolated DB test files; `npm run typecheck` and `npm run build` passed (Vite production build and Wrangler `4.137.0` dry-run); and WSL `git diff --check` passed. The temporary dependency symlink was removed and the task worktree was clean. API-01 remains in progress for the database-backed public reader and live spatial filtering; no live data, source rights, or deployment behavior is established by this slice. See the [assignment and handoff](assignments/API-GEOJSON-ROUTE-CORE.md).

### UI-01-FEED-FILTERS-CORE assigned — 26 September 2026

Root selected a bounded UI-only feed task after the synthetic GeoJSON route was accepted. The assignment adds category, lifecycle, and freshness filters over the records already loaded by the existing API page, retains free-text search, shows filtered counts and a clear action, and resets map selection when the visible record set changes. It explicitly labels the limited loaded-page scope and states that no match does not establish safety. It does not change API/contracts or invent fixtures, geometry, or live data. The assigned branch is `work/UI-01-FEED-FILTERS-CORE`; the completed GeoJSON managed worktree is reused after verification that it is clean. See [assignment](assignments/UI-01-FEED-FILTERS-CORE.md).

### UI-01-FEED-FILTERS-CORE accepted — 26 September 2026

Luna Max implemented the assigned UI slice on `work/UI-01-FEED-FILTERS-CORE`. Implementation commit `3d00b29c6fa144e2f7304da4af245c60d253562b` (`feat(UI-01): add loaded-page feed filters`) and handoff commit `76aba84577a1be1a0bb4ed5e678f572f31779f6a` (`docs(UI-01): record feed filter handoff`) were fast-forwarded to local `main` after root review. The changed product paths are `apps/web/src/App.tsx`, `apps/web/src/EventFeed.tsx`, `apps/web/src/styles.css`, and `apps/web/test/ui.test.tsx`. Category, lifecycle, freshness, and free-text filters combine with AND semantics over already-loaded API records; matching/loaded counts, clear-all, selection reset, and separate filtered-empty/API-empty/loading/unavailable states are covered. The filtered-empty copy does not imply safety. Synthetic demo labels and evidence/time display remain intact.

Root independently ran WSL Ubuntu-26.04 using Node.js `v24.21.0` and npm `11.19.0`: focused UI tests passed 9/9; full `npm test` passed 249/249 (web 22, Worker 137, DB 78 across 10 isolated files, casebook 12); `npm run typecheck`, `npm run build` (Vite production build and Wrangler dry-run), and `git diff --check` passed. Screenshot review could not be performed because WSL has no installed headless browser or Playwright/Puppeteer package; none was installed. No API/DTO, dependency, migration, configuration, source, hosted provider, or external service changed. The filters cover only the loaded demo page, not a complete incident census or live conditions. See the [assignment and root review](assignments/UI-01-FEED-FILTERS-CORE.md).

### L1-FIXTURE-RUNNER-CORE assigned — 26 September 2026

The accepted JOB-01 queue can lease any due row, while L1-FIXTURE-PIPE-CORE deliberately accepts only an already-leased synthetic moderator submission. Root assigned a bounded coordinator and pre-lease SQL filter to compose those modules safely: one due synthetic moderator-submission job per invocation, exact in-memory fixture catalog, caller-supplied time, and stable bounded outcomes. The slice adds no HTTP fetching, source poll handling, moderator enqueue/authentication, Cron/Workflow binding, DB driver, model, publication, or live-source access. Work is assigned to `work/L1-FIXTURE-RUNNER-CORE` in its own worktree; see [assignment](assignments/L1-FIXTURE-RUNNER-CORE.md).

### L1-FIXTURE-RUNNER-CORE accepted — 26 September 2026

Root reviewed and fast-forwarded `work/L1-FIXTURE-RUNNER-CORE` to `main` at handoff `bb96f0a182d09064d67817439196e8b153a3d1a3` (`a9dcde633b9bde0bed06680df5035dd13bbe2ac4` implementation). The queue filters synthetic moderator submissions before leasing, while the generic claim method remains unchanged. The injected runner claims at most one due job using a caller-supplied timestamp and composes the exact-catalog L1 processor. It returns only bounded outcomes/counts, preserves retry, terminal, empty-input and lost-lease behavior, and does not create event or publication rows.

Root independently verified in WSL Ubuntu-26.04 with Node.js `v24.21.0` and npm `11.19.0`: focused queue/runner/pipeline tests passed 21/21; `npm run db:test` passed all 10 isolated files; full `npm test` passed 259/259 (web 22, Worker 145, DB 80, evaluation 12); `npm run typecheck`, `npm run build` (Vite production and Wrangler `4.137.0` dry-run), and WSL `git diff --check` passed. No migration, dependency, runtime binding, route, Cron, external service, live source, or deployment was added. Hosted PostgreSQL/Neon behavior remains unverified; the assignment and root acceptance are recorded in [L1-FIXTURE-RUNNER-CORE](assignments/L1-FIXTURE-RUNNER-CORE.md).

### API-PUBLIC-LOOKUPS-CORE assigned — 26 September 2026

ADR-012's strict public projector requires trusted scope-name and exact source-attribution lookup inputs, but DATA-01 currently persists neither a reviewed public-name catalog nor explicit public-display permission for an exact evidence span. Root accepted ADR-019 and assigned a local schema/read-port foundation for those inputs. The task adds append-only review snapshots and least-privilege public lookup views, using only authored synthetic PGlite rows. It does not add a route, approval writer, real permission record, excerpt, live data, or runtime provider binding. See [ADR-019](decisions/ADR-019-public-projection-lookups.md) and [assignment](assignments/API-PUBLIC-LOOKUPS-CORE.md).

### API-PUBLIC-LOOKUPS-CORE accepted — 26 September 2026

Root reviewed and fast-forwarded `work/API-PUBLIC-LOOKUPS-CORE` to `main` at handoff `0f5e7f2463b424857a552df880da59089e16e05c` (`535efebd52045856441a2297b24678ae0e250f75` implementation). Root review confirmed the append-only review histories, exact evidence composite foreign key, latest-approved safe views, reader-only SELECT grants, parameterized bounded lookups, and RFC 3339 UTC timestamp output. A separate root review commit, `b69cda0` (`test(API-PUBLIC-LOOKUPS-CORE): follow additive migration version`), changed two stale test assertions to verify migration 010 is present without requiring it to be the newest migration.

Root independently verified in WSL Ubuntu-26.04 with Node.js `v24.21.0`, npm `11.19.0`, and Git `2.53.0`: focused lookup tests passed 6/6; `npm run db:test` passed 11/11 isolated files; full `npm test` passed 265/265 (web 22, Worker 145, DB 86, evaluation 12); `npm run typecheck`, `npm run build` (Vite production build and Wrangler `4.137.0` dry-run), and WSL `git diff --check` passed. The fixtures are authored and synthetic, even where a `live` marker is used to exercise filtering. No approval writer, source-rights record, live source, route, provider binding, dependency, or deployment was added. Hosted Neon/PostgreSQL behavior remains unverified; actual review records require MOD-01 authorization and data permissions. See the [assignment and root acceptance](assignments/API-PUBLIC-LOOKUPS-CORE.md).

### API-PUBLIC-SNAPSHOT-CORE assigned — 26 September 2026

With reviewed lookup views in place, root assigned a bounded read-only database port for one current live published-event snapshot and its exact referenced impacts. It reads only the existing `public_event_versions` and `public_event_impacts` views, filters the live dataset explicitly, and uses authored PGlite rows. The slice adds no API route, public DTO, database migration, Worker/database runtime wiring, history, or geometry behavior. See [assignment](assignments/API-PUBLIC-SNAPSHOT-CORE.md).

### API-PUBLIC-SNAPSHOT-CORE accepted — 26 September 2026

Root reviewed and fast-forwarded `work/API-PUBLIC-SNAPSHOT-CORE` to local `main` at handoff `cac9e2aedb9361f351e2cf379291503eac81f78f` (`6a355bc24f00bbdb37fba34520cb2176df03531a` implementation). The reader accesses only the existing safe public event and impact views, explicitly filters current live rows, ties impacts to the exact event version, validates returned rows, and rejects result overflow. Its internal result is bounded to `found` or `missing`; it is not yet connected to an HTTP or public projection path.

Root independently verified in WSL Ubuntu-26.04 with Node.js `v24.21.0`, npm `11.19.0`, and Git `2.53.0`: focused PGlite tests passed 6/6; `npm run db:test` passed 12/12 isolated files (92 tests); full `npm test` passed 271/271 (web 22, Worker 145, DB 92, evaluation 12); `npm run typecheck`, `npm run build` (Vite production and Wrangler `4.137.0` dry-run), and `git diff --check` passed. Hosted Neon/PostgreSQL behavior remains unverified. No migration, route, DTO, Worker runtime DB binding, live source, provider setting, or external service changed. See [assignment and root review](assignments/API-PUBLIC-SNAPSHOT-CORE.md).

### API-PUBLIC-PROJECTION-SERVICE-CORE assigned — 26 September 2026

Root assigned a bounded Layer 4 composition service for the accepted snapshot and reviewed lookup readers. It first binds requested event and impact envelope IDs/versions to their untrusted payloads, then derives and caps scope-name keys across event, claim, and impact scopes plus attribution keys for claim `support` references only. It sends only those keys to the lookup port and passes the assembled internal snapshot through the existing strict `projectPublicEvent` allowlist. The service must return only `EventView` or `missing`, fail closed on mismatches or unresolved/ambiguous lookups, and use authored fictional fixtures. No route, public contract, database adapter/runtime binding, migration, source, or provider behavior is in scope. See [assignment](assignments/API-PUBLIC-PROJECTION-SERVICE-CORE.md).

### API-PUBLIC-PROJECTION-SERVICE-CORE accepted — 26 September 2026

Root reviewed and fast-forwarded `work/API-PUBLIC-PROJECTION-SERVICE-CORE` to local `main` at handoff `eae394bf44bdb2d6e7116deef0437dedb327364b` (`3adcc0106cec34bf77a4e794f125bfb656a36098` implementation, `58727dd08080ca760842a351de7dc9b88e59e77f` excerpt guard). Review confirmed request/event/impact identity and version validation precedes lookup, scope keys include event, claim, and impact scopes but omit geometry IDs, attribution keys derive only from claim `support` references, and lookup rows cannot disclose excerpts. Root independently verified in WSL Ubuntu-26.04 with Node.js `v24.21.0`, npm `11.19.0`, and Git `2.53.0`: focused tests 38/38; full `npm test` 309/309 (web 22, Worker 183, DB 92 across 12 files, evaluation 12); `npm run typecheck`; `npm run build` including Vite and Wrangler `4.137.0` dry-run; and `git diff --check` all passed. Hosted Neon/PostgreSQL behavior and HTTP/runtime integration remain unverified. No migration, route, DTO, runtime binding, dependency, source, or external service changed. See [assignment and root acceptance](assignments/API-PUBLIC-PROJECTION-SERVICE-CORE.md).

### API-PUBLIC-GEOMETRY-READER-CORE assigned — 26 September 2026

Root assigned a bounded database-only read path for stored geometries referenced by current published live events. A security-barrier view will expose only exact IDs referenced by current event/claim scopes, and an injected repository will retrieve at most 500 exact IDs. Tests use authored PGlite fixtures and verify that historical, synthetic, unpublished, withdrawn, and unreferenced rows stay unavailable to the public-reader role. No route, L4 composition, Worker runtime binding, public contract, live source, or source-backed geometry is added. See [assignment](assignments/API-PUBLIC-GEOMETRY-READER-CORE.md).

### API-PUBLIC-GEOMETRY-READER-CORE accepted — 26 September 2026

Root reviewed and fast-forwarded `work/API-PUBLIC-GEOMETRY-READER-CORE` to local `main` at handoff `8129dd7b2bb135e94a7d02579f0df88b07624df3` (`04a43b2b9ce1866fe2953c2d4a8e76df7a689fe8` implementation). Migration 012 adds a `security_barrier` view exposing only live geometry IDs referenced by the current published event or by a matching published claim and normalized claim-geometry link. The public-reader role receives only the view grant, with direct geometry/evidence base-table reads denied. The injected repository accepts exact unique IDs up to 500, skips SQL for an empty list, uses a parameterized query, validates result identity, sorts deterministically, and emits stable redacted failures. The existing L4 projector remains authoritative for full geometry, CRS84, topology and evidence-support checks.

Root independently verified the focused geometry test (8/8), full WSL suite (317/317: web 22, Worker 183, DB 100 across 13 files, evaluation 12), typecheck, Vite production build, Wrangler `4.137.0` dry-run, and `git diff --check`. The task agent also passed `npm run db:test` (13/13 files) and the full suite. No dependency, route, public contract, Worker database binding, source, or runtime configuration changed. PGlite fixtures are authored fiction; hosted Neon privileges and Worker/runtime integration remain unverified. See the [assignment and handoff](assignments/API-PUBLIC-GEOMETRY-READER-CORE.md).

### API-PUBLIC-DETAIL-PROJECTION-CORE assigned — 26 September 2026

Root assigned the next bounded Layer 4 slice to compose the accepted current-event snapshot, reviewed lookup and geometry-reader ports into the existing `EventDetail` allowlist. It derives only bounded event/claim geometry references and delegates schema, CRS84 and exact evidence-support validation to `projectPublicEventDetail`. This remains an injected read-only service with authored fictional tests; it adds no route, public contract, database/runtime binding, source or migration. See [assignment](assignments/API-PUBLIC-DETAIL-PROJECTION-CORE.md).

### API-PUBLIC-DETAIL-PROJECTION-CORE accepted — 26 September 2026

Root reviewed and fast-forwarded `work/API-PUBLIC-DETAIL-PROJECTION-CORE` to local `main` at handoff `58edcf1d07630011c67f679adad5b8896b1b19d9` (`efed7c22208c3740dff971eb9ec4febe07f3c98f` implementation). The injected read-only Layer 4 service validates the current snapshot and exact reviewed lookups, collects only unique event/claim geometry IDs, applies the 500-reference cap, skips geometry I/O for empty references, and passes untrusted records through the strict `EventDetail` projector. The existing EventView service retains the same behavior through a shared snapshot preparation helper.

Root independently passed focused tests 32/32, full suite 317/317 (web 22, Worker 183, DB 100 across 13 files, evaluation 12), `npm run typecheck`, `npm run build` (Vite production and Wrangler dry-run), and WSL `git diff --check`. The agent also passed those checks. The focused test is run separately because the existing Worker test script enumerates paths and package-script edits were outside the assignment. No migration, dependency, public contract, route, Worker binding or source was added. Fixtures are authored fiction; hosted Neon, source rights, runtime binding and live geometry remain unverified. See the [assignment and handoff](assignments/API-PUBLIC-DETAIL-PROJECTION-CORE.md).

### API-PUBLIC-HISTORY-READER-CORE assigned — 26 September 2026

Root assigned a bounded DB foundation for reading published versions only while an event has a current public version. The view and keyset reader will expose internal schema 2.0 records to the public-reader role through one filtered view; later L4 work must construct safe history summaries. The task explicitly excludes withdrawn events, public retraction semantics, route/runtime wiring, and source data. See [assignment](assignments/API-PUBLIC-HISTORY-READER-CORE.md).
